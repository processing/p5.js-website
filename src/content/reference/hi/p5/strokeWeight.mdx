---
title: strokeWeight
module: आकार
submodule: गुण
file: src/core/shape/attributes.js
description: >
  <p>बिंदुओं, रेखाओं और आकृतियों की रूपरेखा के लिए उपयोग किए जाने वाले स्ट्रोक की चौड़ाई निर्धारित करता है।</p>

  <p>ध्यान दें: <code>strokeWeight()</code> परिवर्तनों से प्रभावित होता है, विशेष रूप से <a href="/reference/p5/scale">scale()</a> पर कॉल।</p>
line: 540
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Top.
      line(20, 20, 80, 20);

      // Middle.
      strokeWeight(4);
      line(20, 40, 80, 40);

      // Bottom.
      strokeWeight(10);
      line(20, 70, 80, 70);

      describe('Three horizontal black lines. The top line is thin, the middle is medium, and the bottom is thick.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Top.
      line(20, 20, 80, 20);

      // Scale by a factor of 5.
      scale(5);

      // Bottom. Coordinates are adjusted for scaling.
      line(4, 8, 16, 8);

      describe('Two horizontal black lines. The top line is thin and the bottom is five times thicker than the top.');
    }
    </code>
    </div>
class: p5
params:
  - name: weight
    description: |
      <p>स्ट्रोक का वजन (पिक्सेल में).</p>
    type: Number
chainable: true
---


# strokeWeight
