---
title: strokeCap
module: आकार
submodule: गुण
file: src/core/shape/attributes.js
description: >
  <p>पंक्तियों के सिरों को प्रस्तुत करने के लिए शैली सेट करता है।</p>

  <p>पंक्ति के अंत के कैप या तो गोलाकार (<code>ROUND</code>), वर्गाकार (<code>SQUARE</code>), या विस्तारित (<code>PROJECT</code>) होते हैं। डिफ़ॉल्ट सीमा <code>ROUND</code> है।</p>

  <p> <code>strokeCap()</code> को दिया गया तर्क सभी बड़े अक्षरों में लिखा जाना चाहिए क्योंकि स्थिरांक <code>ROUND</code>, <code>SQUARE</code>, और
  <code>PROJECT</code> को इस प्रकार परिभाषित किया गया है।

  जावास्क्रिप्ट एक केस-सेंसिटिव भाषा है।</p>
line: 387
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      strokeWeight(12);

      // Top.
      strokeCap(ROUND);
      line(20, 30, 80, 30);

      // Middle.
      strokeCap(SQUARE);
      line(20, 50, 80, 50);

      // Bottom.
      strokeCap(PROJECT);
      line(20, 70, 80, 70);

      describe(
        'Three horizontal lines. The top line has rounded ends, the middle line has squared ends, and the bottom line has longer, squared ends.'
      );
    }
    </code>
    </div>
class: p5
params:
  - name: cap
    description: |
      <p>या तो गोल, चौकोर, या प्रोजेक्ट</p>
    type: Constant
chainable: true
---


# strokeCap
