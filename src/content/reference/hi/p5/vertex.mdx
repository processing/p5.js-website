---
title: vertex
module: आकार
submodule: शीर्ष
file: src/core/shape/vertex.js
description: >
  <p>सभी आकृतियों का निर्माण शीर्षों की एक श्रृंखला को जोड़कर किया जाता है।

  <a href='/reference/p5/vertex'>vertex()</a> का उपयोग बिंदुओं, रेखाओं, त्रिभुजों, चतुर्भुजों और बहुभुजों के लिए शीर्ष निर्देशांक निर्दिष्ट करने के लिए किया जाता है। इसका उपयोग विशेष रूप से <a href='/reference/p5/beginShape'>beginShape()</a> और <a href='/reference/p5/endShape'>endShape()</a> फ़ंक्शंस के भीतर किया जाता है।</p>
line: 930
isConstructor: false
itemtype: तरीका
alt: |-
  कैनवास के मध्य-दाहिनी ओर चौकोर आकार में 4 काले बिंदु।
  हीरे की आकृति बनाने वाले 4 बिंदु।
  8 अंक एक सितारा बनाते हैं।
  8 बिंदु 4 रेखाएँ बनाते हैं।
  बीच में एक खोखले खंड के साथ एक घूमने वाली 3D आकृति।
example:
  - |-

    <div>
    <code>
    strokeWeight(3);
    beginShape(POINTS);
    vertex(30, 20);
    vertex(85, 20);
    vertex(85, 75);
    vertex(30, 75);
    endShape();
    </code>
    </div>

    <div>
    <code>
    createCanvas(100, 100, WEBGL);
    background(240, 240, 240);
    fill(237, 34, 93);
    noStroke();
    beginShape();
    vertex(0, 35);
    vertex(35, 0);
    vertex(0, -35);
    vertex(-35, 0);
    endShape();
    </code>
    </div>

    <div>
    <code>
    createCanvas(100, 100, WEBGL);
    background(240, 240, 240);
    fill(237, 34, 93);
    noStroke();
    beginShape();
    vertex(-10, 10);
    vertex(0, 35);
    vertex(10, 10);
    vertex(35, 0);
    vertex(10, -8);
    vertex(0, -35);
    vertex(-10, -8);
    vertex(-35, 0);
    endShape();
    </code>
    </div>

    <div>
    <code>
    strokeWeight(3);
    stroke(237, 34, 93);
    beginShape(LINES);
    vertex(10, 35);
    vertex(90, 35);
    vertex(10, 65);
    vertex(90, 65);
    vertex(35, 10);
    vertex(35, 90);
    vertex(65, 10);
    vertex(65, 90);
    endShape();
    </code>
    </div>

    <div>
    <code>
    // Click to change the number of sides.
    // In WebGL mode, custom shapes will only
    // display hollow fill sections when
    // all calls to vertex() use the same z-value.

    let sides = 3;
    let angle, px, py;

    function setup() {
      createCanvas(100, 100, WEBGL);
      setAttributes('antialias', true);
      fill(237, 34, 93);
      strokeWeight(3);
    }

    function draw() {
      background(200);
      rotateX(frameCount * 0.01);
      rotateZ(frameCount * 0.01);
      ngon(sides, 0, 0, 80);
    }

    function mouseClicked() {
      if (sides > 6) {
        sides = 3;
      } else {
        sides++;
      }
    }

    function ngon(n, x, y, d) {
      beginShape(TESS);
      for (let i = 0; i < n + 1; i++) {
        angle = TWO_PI / n * i;
        px = x + sin(angle) * d / 2;
        py = y - cos(angle) * d / 2;
        vertex(px, py, 0);
      }
      for (let i = 0; i < n + 1; i++) {
        angle = TWO_PI / n * i;
        px = x + sin(angle) * d / 4;
        py = y - cos(angle) * d / 4;
        vertex(px, py, 0);
      }
      endShape();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 930
    params:
      - name: x
        description: |
          <p>शीर्ष का x-कोआर्डिनेट</p>
        type: संख्या
      - name: "y"
        description: |
          <p>शीर्ष का y-कोआर्डिनेट</p>
        type: संख्या
    chainable: 1
  - line: 1061
    params:
      - name: x
        description: ""
        type: संख्या
      - name: "y"
        description: ""
        type: संख्या
      - name: z
        description: |
          <p>शीर्ष का z-कोआर्डिनेट।यदि निर्दिष्ट नहीं है तो यह 0 पर डिफॉल्ट हो जाता है।</p>
        type: संख्या
        optional: true
    chainable: 1
  - line: 1069
    params:
      - name: x
        description: ""
        type: संख्या
      - name: "y"
        description: ""
        type: संख्या
      - name: z
        description: ""
        type: संख्या
        optional: true
      - name: u
        description: |
          <p>शीर्ष की बनावट के लिए u-कोआर्डिनेट</p>
        type: संख्या
        optional: true
      - name: v
        description: |
          <p>शीर्ष की बनावट के लिए v-कोआर्डिनेट</p>
        type: संख्या
        optional: true
    chainable: 1
chainable: true
---

# vertex
