---
title: keyTyped
module: आयोजन
submodule: कीबोर्ड
file: src/events/keyboard.js
description: >
  <p>एक फ़ंक्शन जिसे प्रिंट करने योग्य वर्णों वाली कुंजियाँ होने पर एक बार कॉल किया जाता है
  दबाया.</p>

  <p>फ़ंक्शन घोषित करना <code>keyTyped()</code> चलाने के लिए एक code ब्लॉक सेट करता है
  एक बार

  स्वचालित रूप से जब उपयोगकर्ता मुद्रण योग्य वर्ण वाली कोई कुंजी दबाता है

  जैसे <code>a</code> या 1. संशोधक कुंजियाँ जैसे <code>SHIFT</code>,
  <code>नियंत्रण</code>, और तीर कुंजियाँ

  नजरअंदाज कर दिया जाएगा:</p>

  <code>

  फ़ंक्शन कुंजी टाइप किया गया() {
    // चलाने के लिए code।
  }

  </code>


  <p><a href="/reference/p5/key">कुंजी</a> और <a
  href='/reference/p5/keyCode'>keyCode</a>

  वेरिएबल को हाल ही में जारी किए गए मान के साथ अद्यतन किया जाएगा

  <code>keyTyped()</code> को p5.js द्वारा कॉल किया जाता है:</p>

  <code>

  फ़ंक्शन कुंजी टाइप किया गया() {
    // कुंजी का उपयोग करके "सी" वर्ण की जांच करें।
    यदि (कुंजी === 'सी') {
      // चलाने के लिए code।
    }

  <p> // keyCode का उपयोग करके "c" की जांच करें।
    if(keycode === 67) {
      // चलाने के लिए code।
    }
  }

  </p></code>

  <p>पैरामीटर, <code>इवेंट</code>, वैकल्पिक है। <code>keyTyped()</code> है
  हमेशा एक पारित किया

  <a href='https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent'
  target='_blank'>कीबोर्डइवेंट</a>

  गुणों वाली वस्तु जो कुंजी प्रेस घटना का वर्णन करती है:</p>

  <code>

  फ़ंक्शन कुंजी जारी(घटना) {
    // चलाने के लिए code जो ईवेंट का उपयोग करता है।
    कंसोल.लॉग(इवेंट);
  }

  </code>


  <p>नोट: <a href="/reference/p5/keyPressed">keyPressed()</a> फ़ंक्शन का उपयोग करें
  और

  <a href="/reference/p5/keyCode">keyCode</a> सिस्टम वेरिएबल का जवाब देना है
  संशोधक

  <code>ALT</code> जैसी कुंजियाँ।</p>

  <p>ब्राउज़र में विभिन्न प्रमुख घटनाओं से जुड़े डिफ़ॉल्ट व्यवहार हो सकते हैं। को

  इस ईवेंट के लिए किसी भी डिफ़ॉल्ट व्यवहार को रोकें, इसमें <code> return false;</code> जोड़ें
  समाप्त

  समारोह का.</p>
line: 639
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // Click on the canvas to begin detecting key presses.
    // Note: Pressing special keys such as SPACE have no effect.

    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'A gray square with a white square at its center. The inner square changes color when the user presses a key.'
      );
    }

    function draw() {
      background(200);

      // Style the square.
      fill(value);

      // Draw the square.
      square(25, 25, 50);
    }

    // Toggle the square's color when the user types a printable key.
    function keyTyped() {
      if (value === 0) {
        value = 255;
      } else {
        value = 0;
      }
      // Uncomment to prevent any default behavior.
      // return false;
    }
    </code>
    </div>

    <div>
    <code>
    // Click on the canvas to begin detecting key presses.

    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'A gray square with a white square at its center. The inner square turns black when the user types the "b" key. It turns white when the user types the "a" key.'
      );
    }

    function draw() {
      background(200);

      // Style the square.
      fill(value);

      // Draw the square.
      square(25, 25, 50);
    }

    // Reassign value when the user types the 'a' or 'b' key.
    function keyTyped() {
      if (key === 'a') {
        value = 255;
      } else if (key === 'b') {
        value = 0;
      }
      // Uncomment to prevent any default behavior.
      // return false;
    }
    </code>
    </div>
class: p5
params:
  - name: event
    description: |
      <p>optional <code>KeyboardEvent</code> callback argument.</p>
    type: KeyboardEvent
    optional: true
chainable: false
---


# keyTyped
