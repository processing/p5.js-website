---
title: movedX
module: आयोजन
submodule: माउस
file: src/events/mouse.js
description: >
  <p>एक <code>संख्या</code> सिस्टम वैरिएबल जो माउस के क्षैतिज को ट्रैक करता है
  आंदोलन.</p>

  <p><code>movedX</code> यह ट्रैक करता है कि माउस कितने पिक्सेल पर बाएँ या दाएँ घूमता है
  बीच में

  तख्ते. यदि माउस बायीं ओर चलता है तो <code>movedX</code> का मान ऋणात्मक होगा
  बीच में

  यदि यह दाईं ओर चलता है तो फ्रेम और एक सकारात्मक मान। <code>movedX</code> हो सकता है
  गणना

  <code>mouseX - pmouseX</code> के रूप में।</p>

  <p>ध्यान दें: <code>movedX</code> तब भी अपडेट होता रहता है

  <a href='/reference/p5/requestPointerLock'>requestPointerLock()</a> है
  सक्रिय.</p>
line: 12
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        'A gray square. The text ">>" appears when the user moves the mouse to the right. The text "<<" appears when the user moves the mouse to the left.'
      );
    }

    function draw() {
      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display >> when movedX is positive and
      // << when it's negative.
      if (movedX > 0) {
        text('>>', 50, 50);
      } else if (movedX < 0) {
        text('<<', 50, 50);
      }
    }
    </code>
    </div>
class: p5
type: Number
---


# movedX
