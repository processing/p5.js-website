---
title: acos
module: गणित
submodule: त्रिकोणमिति
file: src/math/trigonometry.js
description: >
  <p>किसी संख्या की चाप कोज्या की गणना करता है।</p>

  <p><code>acos()</code>, <a का व्युत्क्रम है
   href='/reference/p5/cos'>cos()</a>. यह -1 से 1 की सीमा में तर्कों की अपेक्षा करता है। डिफ़ॉल्ट रूप से, 
   <code>acos()</code> 0 से π (लगभग 3.14) की सीमा में मान लौटाता है। यदि <a href="/reference/p5/angelMode">angleMode()</a> 
   <code>DEGREES</code> है, तो मान 0 से 180 की सीमा में लौटाए जाते हैं।</p>
line: 18
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate cos() and acos() values.
      let a = PI;
      let c = cos(a);
      let ac = acos(c);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(c, 3)}`, 35, 50);
      text(`${round(ac, 3)}`, 35, 75);

      describe('The numbers 3.142, -1, and 3.142 written on separate rows.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate cos() and acos() values.
      let a = PI + QUARTER_PI;
      let c = cos(a);
      let ac = acos(c);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(c, 3)}`, 35, 50);
      text(`${round(ac, 3)}`, 35, 75);

      describe('The numbers 3.927, -0.707, and 2.356 written on separate rows.');
    }
    </code>
    </div>
class: p5
params:
  - name: value
    description: |
      <p>वह मान जिसकी चाप कोज्या लौटाई जानी है।</p>
    type: संख्या
return:
  description: दिए गए मान की चाप कोज्या.
  type: संख्या
chainable: false
---


# acos
