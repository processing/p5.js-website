---
title: updatePixels
module: छवि
submodule: पिक्सल
file: src/image/pixels.js
description: >
  <p>कैनवस को RGBA मानों के साथ अपडेट करता है

  <a href="/reference/p5/पिक्सेल">पिक्सेल</a> सरणी।</p>

  <p><code>updatePixels()</code> को केवल मान बदलने के बाद कॉल करने की आवश्यकता है
  में

  <a href='/reference/p5/पिक्सेल'>पिक्सेल</a> सरणी। ऐसे बदलाव किये जा सकते हैं
  सीधे

  <a href="/reference/p5/loadPixels">loadPixels()</a> पर कॉल करने के बाद
  कॉलिंग

  <a href='/reference/p5/set'>set()</a>.</p>
line: 1118
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // Load the image.
    function preload() {
      img = loadImage('assets/rockies.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // Display the image.
      image(img, 0, 0, 100, 100);

      // Get the pixel density.
      let d = pixelDensity();

      // Calculate the halfway index in the pixels array.
      let halfImage = 4 * (d * width) * (d * height / 2);

      // Load the pixels array.
      loadPixels();

      // Copy the top half of the canvas to the bottom.
      for (let i = 0; i < halfImage; i += 1) {
        pixels[i + halfImage] = pixels[i];
      }

      // Update the canvas.
      updatePixels();

      describe('Two identical images of mountain landscapes, one on top of the other.');
    }
    </code>
    </div>
class: p5
params:
  - name: x
    description: |
      <p>क्षेत्र के ऊपरी-बाएँ कोने का x-निर्देशांक
                              अद्यतन करने के लिए.</p>
    type: Number
    optional: true
  - name: 'y'
    description: |
      <p>क्षेत्र के ऊपरी-बाएँ कोने का y-निर्देशांक
                              अद्यतन करने के लिए.</p>
    type: Number
    optional: true
  - name: w
    description: |
      <p>अद्यतन करने के लिए क्षेत्र की चौड़ाई।</p>
    type: Number
    optional: true
  - name: h
    description: |
      <p>अद्यतन करने के लिए क्षेत्र की ऊंचाई।</p>
    type: Number
    optional: true
chainable: false
---


# updatePixels