---
title: pwinMouseY
module: आयोजन
submodule: माउस
file: src/events/mouse.js
description: >
  <p>एक <code>नंबर</code> वेरिएबल जो माउस के पिछले वर्टिकल को ट्रैक करता है
  पद

  ब्राउज़र के भीतर.</p>

  <p><code>pwinMouseY</code> के सापेक्ष माउस की स्थिति पर नज़र रखता है
  बाएं से बाएं

  ब्राउज़र विंडो का कोना. इसका मूल्य है

  <a href="/reference/p5/winMouseY">winMouseY</a> पिछले फ्रेम से। के लिए

  उदाहरण के लिए, यदि माउस 50 पिक्सेल से था

  फिर, अंतिम फ़्रेम के दौरान ब्राउज़र का शीर्ष किनारा
  <code>pwinMouseY</code> होगा

  50 हो.</p>

  <p>टचस्क्रीन डिवाइस पर, <code>pwinMouseY</code> y-कोऑर्डिनेट को होल्ड करेगा
  सबसे का

  हालिया स्पर्श बिंदु. <code>pwinMouseY</code> को वर्तमान पर रीसेट कर दिया गया है

  प्रत्येक के प्रारंभ में <a href="/reference/p5/winMouseY">winMouseY</a> मान
  छूना

  घटना.</p>

  <p>ध्यान दें: माउस को ट्रैक करने के लिए <a href="/reference/p5/pmouseY">pmouseY</a> का उपयोग करें
  पहले का

  कैनवास के भीतर y-समन्वय।</p>
line: 622
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Slow the frame rate.
      frameRate(10);

      describe('A gray square. A white circle at its center grows larger when the mouse moves vertically.');
    }

    function draw() {
      background(200);

      // Calculate the circle's diameter.
      let d = winMouseY - pwinMouseY;

      // Draw the circle.
      circle(50, 50, d);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      // Create the canvas and set its position.
      let cnv = createCanvas(100, 100);
      cnv.position(20, 20);

      describe('A gray square with a number at its center. The number changes as the user moves the mouse vertically.');
    }

    function draw() {
      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display pwinMouseY.
      text(pwinMouseY, 50, 50);
    }
    </code>
    </div>
class: p5
type: Number
---


# pwinMouseY
