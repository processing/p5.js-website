---
title: tan
module: गणित
submodule: त्रिकोणमिति
file: src/math/trigonometry.js
description: >
  <p>किसी कोण की स्पर्श रेखा की गणना करता है।</p>

  <p><code>tan()</code> रचनात्मक कोडिंग में कई ज्यामितीय कार्यों के लिए उपयोगी है। लौटाए गए मान -अनंत से अनंत तक होते हैं और इनपुट कोण बढ़ने पर समय-समय पर दोहराए जाते हैं। 
  <code>tan()</code> वर्तमान <a href="/reference/p5/angelMode">angleMode()</a> 
  को ध्यान में रखता है।</p>
line: 441
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('A series of identical curves drawn with black dots. Each curve starts from the top of the canvas, continues down at a slight angle, flattens out at the middle of the canvas, then continues to the bottom.');
    }

    function draw() {
      // Calculate the coordinates.
      let x = frameCount;
      let y = 5 * tan(x * 0.1) + 50;

      // Draw the point.
      point(x, y);
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: |
      <p>कोण.</p>
    type: संख्या
return:
  description: कोण की स्पर्शरेखा.
  type: संख्या
chainable: false
---


# tan
