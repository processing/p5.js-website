---
title: focused
module: पर्यावरण
submodule: पर्यावरण
file: src/core/environment.js
description: >
  <p>एक <code>Boolean</code> चर जो <code>true</code> है अगर ब्राउज़र केंद्रित है और <code>false</code> है अगर नहीं।</p>

  <p>ध्यान दें: ब्राउज़र विंडो केवल तभी इनपुट प्राप्त कर सकती है जब वह केंद्रित हो।</p>
line: 174
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // Open this example in two separate browser
    // windows placed side-by-side to demonstrate.

    function setup() {
      createCanvas(100, 100);

      describe('A square changes color from green to red when the browser window is out of focus.');
    }

    function draw() {
      // Change the background color
      // when the browser window
      // goes in/out of focus.
      if (focused === true) {
        background(0, 255, 0);
      } else {
        background(255, 0, 0);
      }
    }
    </code>
    </div>
class: p5
type: Boolean
---


# focused
