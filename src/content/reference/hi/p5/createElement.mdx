---
title: createElement
module: डोम
submodule: डोम
file: src/dom/dom.js
description: >
  <p>एक नया <a href="/reference/p5/p5.Element">p5.Element</a> ऑब्जेक्ट बनाता है।</p>

  <p>पहला पैरामीटर, <code>tag</code> स्ट्रिंग एक एचटीएमएल टैग है, जैसे <code>'h5'</code>|</p>

  <p>दूसरा पैरामीटर, <code>content</code>, वैकल्पिक है। यह एक स्ट्रिंग है जो एचटीएमएल कोड को नए तत्व में डालने के लिए सेट करती है। नए तत्वों में डिफ़ॉल्ट रूप से कोई कोड नहीं होता है।</p>
line: 2467
isConstructor: false
itemtype: तरीका
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Create an h5 element with nothing in it.
      createElement('h5');

      describe('A gray square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Create an h5 element with the content "p5*js".
      let h5 = createElement('h5', 'p5*js');

      // Set the element's style and position.
      h5.style('color', 'deeppink');
      h5.position(30, 15);

      describe('The text "p5*js" written in pink in the middle of a gray square.');
    }
    </code>
    </div>
class: p5
params:
  - name: tag
    description: |
      <p>नए तत्व के लिए टैग।</p>
    type: स्ट्रिंग
  - name: content
    description: |
    <p>तत्व में डालने के लिए एचटीएमएल कोड।</p>
    type: स्ट्रिंग
    optional: true
return:
  description: नया <a href='#/p5.Element'>p5.Element</a> ऑब्जेक्ट।
  type: p5.Element
chainable: false
---


# createElement
