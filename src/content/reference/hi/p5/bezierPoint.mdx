---
title: bezierPoint
module: आकार
submodule: वक्र
file: src/core/shape/curves.js
description: >
  <p>इंटरपोलेशन का उपयोग करके बेज़ियर वक्र के साथ कोआर्डिनेट की गणना करता है।</p>

  <p><code>bezierPoint()</code> एंकर और नियंत्रण बिंदुओं का उपयोग करके बेज़ियर वक्र के साथ कोआर्डिनेट की गणना करता है।
  
  यह <a href="/reference/p5/bezier">bezier()</a> फ़ंक्शन के समान क्रम में बिंदुओं की अपेक्षा करता है।

  <code>bezierPoint()</code> एक समय में एक अक्ष पर काम करता है।
  
  एंकर और नियंत्रण बिंदुओं के x-कोआर्डिनेट को पार करने से वक्र पर एक बिंदु के x-कोआर्डिनेट की गणना की जाएगी।
  
  एंकर और नियंत्रण बिंदुओं के y-कोआर्डिनेट को पार करने से वक्र पर एक बिंदु के y-कोआर्डिनेट की गणना की जाएगी।</p>

  <p>पहला पैरामीटर, <code>a</code>, पहले एंकर बिंदु का समन्वय है।</p>

  <p>दूसरा और तीसरा पैरामीटर, <code>b</code> और <code>c</code>, नियंत्रण बिंदुओं के कोआर्डिनेट हैं।</p>

  <p>चौथा पैरामीटर, <code>d</code>, अंतिम एंकर बिंदु का समन्वय है।</p>

  <p>पांचवां पैरामीटर, <code>t</code>, वक्र के साथ प्रक्षेपित होने वाली मात्रा है। 0 पहला एंकर पॉइंट है, 1 दूसरा एंकर पॉइंट है, और 0.5 उनके बीच का आधा रास्ता है।</p>
line: 324
isConstructor: false
itemtype: तरीका
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Set the coordinates for the curve's anchor and control points.
      let x1 = 85;
      let x2 = 10;
      let x3 = 90;
      let x4 = 15;
      let y1 = 20;
      let y2 = 10;
      let y3 = 90;
      let y4 = 80;

      // Style the curve.
      noFill();

      // Draw the curve.
      bezier(x1, y1, x2, y2, x3, y3, x4, y4);

      // Draw circles along the curve's path.
      fill(255);

      // Top-right.
      let x = bezierPoint(x1, x2, x3, x4, 0);
      let y = bezierPoint(y1, y2, y3, y4, 0);
      circle(x, y, 5);

      // Center.
      x = bezierPoint(x1, x2, x3, x4, 0.5);
      y = bezierPoint(y1, y2, y3, y4, 0.5);
      circle(x, y, 5);

      // Bottom-left.
      x = bezierPoint(x1, x2, x3, x4, 1);
      y = bezierPoint(y1, y2, y3, y4, 1);
      circle(x, y, 5);

      describe('A black s-curve on a gray square. The endpoints and center of the curve are marked with white circles.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('A black s-curve on a gray square. A white circle moves back and forth along the curve.');
    }

    function draw() {
      background(200);

      // Set the coordinates for the curve's anchor and control points.
      let x1 = 85;
      let x2 = 10;
      let x3 = 90;
      let x4 = 15;
      let y1 = 20;
      let y2 = 10;
      let y3 = 90;
      let y4 = 80;

      // Draw the curve.
      noFill();
      bezier(x1, y1, x2, y2, x3, y3, x4, y4);

      // Calculate the circle's coordinates.
      let t = 0.5 * sin(frameCount * 0.01) + 0.5;
      let x = bezierPoint(x1, x2, x3, x4, t);
      let y = bezierPoint(y1, y2, y3, y4, t);

      // Draw the circle.
      fill(255);
      circle(x, y, 5);
    }
    </code>
    </div>
class: p5
params:
  - name: a
    description: |
      <p>वक्र पर पहले बिंदु के कोआर्डिनेट</p>
    type: संख्या
  - name: b
    description: |
      <p>वक्र पर पहले नियंत्रण बिंदु के कोआर्डिनेट</p>
    type: संख्या
  - name: c
    description: |
      <p>वक्र पर दूसरे नियंत्रण बिंदु के कोआर्डिनेट</p>
    type: संख्या
  - name: d
    description: |
      <p>वक्र पर दूसरे बिंदु के कोआर्डिनेट</p>
    type: संख्या
  - name: t
    description: |
      <p>0 और 1 के बीच अंतर्वेशित होने वाली राशि</p>
    type: संख्या
return:
  description: वक्र पर बिंदु का कोआर्डिनेट
  type: संख्या
chainable: false
---

# bezierPoint
