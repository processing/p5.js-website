---
title: width
module: Environment
submodule: Environment
file: src/core/environment.js
description: |-
  <p>कैनवास की चौड़ाई को पिक्सेल में संग्रहित करने वाला <code>Number</code> वेरिएबल।</p>

  <p><code>width</code> की डिफ़ॉल्ट मूल्य 100 होता है। <a href="./createCanvas">createCanvas()</a> या <a href="./resizeCanvas">resizeCanvas()</a> को बुलाने पर <code>width</code> का मूल्य बदल जाता है। <a href="./noCanvas">noCanvas()</a> को बुलाने पर इसका मूल्य 0 हो जाता है।</p>
line: '803'
isConstructor: 'false'
itemtype: property
example:
 "- "<div>\n\n<code>\n\nfunction setup() {\n\n  background(200);\n\n\n\n  // कैनवास की चौड़ाई दिखाएं।\n\n  text(width, 42, 54);\n\n\n\n  describe('The number 100 written in black on a gray square.');\n\n}\n\n</code>\n\n</div>\n\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(50, 100);\n\n\n\n  background(200);\n\n\n\n  // कैनवास की चौड़ाई दिखाएं।\n\n  text(width, 21, 54);\n\n\n\n  describe('The number 50 written in black on a gray rectangle.');\n\n}\n\n</code>\n\n</div>\n\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  // कैनवास की चौड़ाई दिखाएं।\n\n  text(width, 42, 54);\n\n\n\n  describe('The number 100 written in black on a gray square. When the mouse is pressed, the square becomes a rectangle and the number becomes 50.');\n\n}\n\n\n\n// यदि माउस दबाया गया है, \n\n// तो कैनवास का आकार बदलें\n\n// और उसकी नई चौड़ाई दिखाएं।\n\nfunction mousePressed() {\n\n  if (mouseX > 0 && mouseX < width && mouseY > 0 && mouseY < height) {\n\n    resizeCanvas(50, 100);\n\n    background(200);\n\n    text(width, 21, 54);\n\n  }\n\n}\n\n</code>\n\n</div>""
class: p5
type: Number
---

# width
