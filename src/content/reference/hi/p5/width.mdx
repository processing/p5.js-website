---
title: width
module: पर्यावरण
submodule: पर्यावरण
file: src/core/environment.js
description: >
  <p>कैनवास की चौड़ाई को पिक्सेल में संग्रहित करने वाला <code>Number</code> वेरिएबल।</p>

  <p><code>width</code> की डिफ़ॉल्ट मूल्य 100 होता है। <a href="./createCanvas">createCanvas()</a> या 
  <a href="./resizeCanvas">resizeCanvas()</a> को बुलाने पर <code>width</code> का मूल्य बदल जाता है। 
  <a href="./noCanvas">noCanvas()</a> को बुलाने पर इसका मूल्य 0 हो जाता है।</p>
line: 803
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      background(200);

      // Display the canvas' width.
      text(width, 42, 54);

      describe('The number 100 written in black on a gray square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(50, 100);

      background(200);

      // Display the canvas' width.
      text(width, 21, 54);

      describe('The number 50 written in black on a gray rectangle.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Display the canvas' width.
      text(width, 42, 54);

      describe('The number 100 written in black on a gray square. When the mouse is pressed, the square becomes a rectangle and the number becomes 50.');
    }

    // If the mouse is pressed, reisze
    // the canvas and display its new
    // width.
    function mousePressed() {
      if (mouseX > 0 && mouseX < width && mouseY > 0 && mouseY < height) {
        resizeCanvas(50, 100);
        background(200);
        text(width, 21, 54);
      }
    }
    </code>
    </div>
class: p5
type: Number
---


# width
