---
title: movedY
module: आयोजन
submodule: माउस
file: src/events/mouse.js
description: >
  <p>एक <code>संख्या</code> सिस्टम वैरिएबल जो माउस के वर्टिकल को ट्रैक करता है
  आंदोलन.</p>

  <p><code>movedY</code> ट्रैक करता है कि माउस कितने पिक्सेल ऊपर या नीचे जाता है
  बीच में

  तख्ते. यदि माउस ऊपर जाता है तो <code>movedY</code> का मान ऋणात्मक होगा
  बीच में

  यदि यह नीचे जाता है तो फ्रेम और एक सकारात्मक मान। <code>स्थानांतरितवाई</code> किया जा सकता है
  गणना

  <code>mouseY - pmouseY</code> के रूप में।</p>

  <p>ध्यान दें: <code>movedY</code> तब भी अपडेट होता रहता है

  <a href='/reference/p5/requestPointerLock'>requestPointerLock()</a> है
  सक्रिय.</p>
line: 57
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        'A gray square. The text "▲" appears when the user moves the mouse upward. The text "▼" appears when the user moves the mouse downward.'
      );
    }

    function draw() {
      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display ▼ when movedY is positive and
      // ▲ when it's negative.
      if (movedY > 0) {
        text('▼', 50, 50);
      } else if (movedY < 0) {
        text('▲', 50, 50);
      }
    }
    </code>
    </div>
class: p5
type: Number
---


# movedY
