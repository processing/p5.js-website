---
title: height
module: Environment
submodule: Environment
file: src/core/environment.js
description: |-
  <p>कैनवास की ऊंचाई को पिक्सेल में संग्रहित करने वाला <code>Number</code> वेरिएबल।</p>

  <p><code>height</code> की डिफ़ॉल्ट मूल्य 100 होता है। <a href="/reference/p5/createCanvas">createCanvas()</a> या <a href="/reference/p5/resizeCanvas">resizeCanvas()</a> को बुलाने पर <code>height</code> का मूल्य बदल जाता है। <a href="/reference/p5/noCanvas">noCanvas()</a> को बुलाने पर इसका मूल्य 0 हो जाता है।</p>
line: '872'
isConstructor: 'false'
itemtype: property
example:
 "- "<div>\n\n<code>\n\nfunction setup() {\n\n  background(200);\n\n\n\n  // कैनवास की ऊंचाई दिखाएं।\n\n  text(height, 42, 54);\n\n\n\n  describe('The number 100 written in black on a gray square.');\n\n}\n\n</code>\n\n</div>\n\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 50);\n\n\n\n  background(200);\n\n\n\n  // कैनवास की ऊंचाई दिखाएं।\n\n  text(height, 42, 27);\n\n\n\n  describe('ग्रे आयत पर काले रंग में लिखा गया संख्या 50।');\n\n}\n\n</code>\n\n</div>\n\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  // कैनवास की ऊंचाई दिखाएं।\n\n  text(height, 42, 54);\n\n\n\n  describe('The number 100 written in black on a gray square. When the mouse is pressed, the square becomes a rectangle and the number becomes 50.');\n\n}\n\n\n\n// यदि माउस दबाया गया है, \n\n// तो कैनवास का आकार बदलें\n\n// और उसकी नई ऊँचाई दिखाएं।\n\nfunction mousePressed() {\n\n  if (mouseX > 0 && mouseX < width && mouseY > 0 && mouseY < height) {\n\n    resizeCanvas(100, 50);\n\n    background(200);\n\n    text(height, 42, 27);\n\n  }\n\n}\n\n</code>\n\n</div>""
class: p5
type: Number
---

# height
