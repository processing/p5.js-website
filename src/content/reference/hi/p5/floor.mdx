---
title: floor
module: गणित
submodule: गणना
file: src/math/calculation.js
description: |
  <p>निकटतम पूर्णांक मान की गणना करता है जो किसी संख्या के मान से कम या उसके बराबर है।</p>
line: 294
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Use RGB color with values from 0 to 1.
      colorMode(RGB, 1);

      noStroke();

      // Draw the left rectangle.
      let r = 0.8;
      fill(r, 0, 0);
      rect(0, 0, 50, 100);

      // Round r down to 0.
      r = floor(r);

      // Draw the right rectangle.
      fill(r, 0, 0);
      rect(50, 0, 50, 100);

      describe('Two rectangles. The one on the left is bright red and the one on the right is black.');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
        <p>संख्या को पूर्णांकित करना है।</p>
    type: संख्या
return:
  description: पूर्णांकित संख्या.
  type: पूर्णांक
chainable: false
---


# floor
