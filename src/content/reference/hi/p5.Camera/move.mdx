---
title: move
module: 3डी
submodule: कैमरा
file: src/webgl/p5.Camera.js
description: >
  <p>अपने उन्मुखता को बदले बिना कैमरा को अपनी "स्थानीय" अक्षों पर ले जाता है।</p> 
  
  <p>पैरामीटर <code>x</code>, <code>y</code> और <code>z</code>, वह दूरी है जितनी कैमरा को ले जाना चाहिए। उदाहरण के लिए, <code>myCamera.move(10, 20, 30)</code> कॉल करने से कैमरा अपने "स्थानीय" स्थान में 10 पिक्सेल दाएं, 20 पिक्सेल नीचे और 30 पिक्सेल पीछे ले जाया जाता है।</p>
line: 2923
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // Click the canvas to begin detecting key presses.

    let cam;

    function setup() {
      createCanvas(100, 100, WEBGL);

      // Create the first camera.
      // Keep its default settings.
      cam = createCamera();

      // Place the camera at the top-right.
      cam.setPosition(400, -400, 800);

      // Point it at the origin.
      cam.lookAt(0, 0, 0);

      describe(
        'A white cube drawn against a gray background. The cube appears to move when the user presses certain keys.'
      );
    }

    function draw() {
      background(200);

      // Move the camera along its "local" axes
      // when the user presses certain keys.
      if (keyIsPressed === true) {

        // Move horizontally.
        if (keyCode === LEFT_ARROW) {
          cam.move(-1, 0, 0);
        }
        if (keyCode === RIGHT_ARROW) {
          cam.move(1, 0, 0);
        }

        // Move vertically.
        if (keyCode === UP_ARROW) {
          cam.move(0, -1, 0);
        }
        if (keyCode === DOWN_ARROW) {
          cam.move(0, 1, 0);
        }

        // Move in/out of the screen.
        if (key === 'i') {
          cam.move(0, 0, -1);
        }
        if (key === 'o') {
          cam.move(0, 0, 1);
        }
      }

      // Draw the box.
      box();
    }
    </code>
    </div>
class: p5.Camera
params:
  - name: x
    description: |
      <p>कैमरे के "स्थानीय" x-अक्ष के साथ चलने के लिए दूरी।</p>
    type: Number
  - name: 'y'
    description: |
      <p>कैमरे के "स्थानीय" y-अक्ष के साथ चलने के लिए दूरी।</p>
    type: Number
  - name: z
    description: |
      <p>कैमरे के "स्थानीय" z-अक्ष के साथ चलने के लिए दूरी।</p>
    type: Number
chainable: false
---


# move
