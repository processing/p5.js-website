---
title: tilt
module: 3D
submodule: कैमरा
file: src/webgl/p5.Camera.js
description: >
  <p>कैमरा को ऊपर और नीचे घुमाता है।</p> 
  
  <p>टिल्टिंग कैमरा की स्थिति को बदलिए बिना कैमरा को घुमाता है। यह घूर्णन कैमरे के "स्थानीय" स्पेस में होता है।</p> <p>पैरामीटर <code>angle</code>, वह कोण है जिस पर कैमरा घूमना चाहिए। एक धनात्मक कोण पास करके, जैसे <code>myCamera.tilt(0.001)</code>, कैमरा को नीचे की ओर घुमाता है। एक ऋणात्मक कोण पास करके, जैसे <code>myCamera.tilt(-0.001)</code>, कैमरा को ऊपर की ओर घुमाता है।</p> 
  
  <p>नोट: कोण वर्तमान <a href="/reference/p5/angleMode">angleMode()</a> के आधार पर व्याख्या किए जाते हैं।</p>
line: 2535
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let cam;
    let delta = 0.001;

    function setup() {
      createCanvas(100, 100, WEBGL);

      // Create a p5.Camera object.
      cam = createCamera();

      // Place the camera at the top-center.
      cam.setPosition(0, -400, 800);

      // Point the camera at the origin.
      cam.lookAt(0, 0, 0);

      describe(
        'A white cube on a gray background. The cube goes in and out of view as the camera tilts up and down.'
      );
    }

    function draw() {
      background(200);

      // Pan with the camera.
      cam.tilt(delta);

      // Switch directions every 120 frames.
      if (frameCount % 120 === 0) {
        delta *= -1;
      }

      // Draw the box.
      box();
    }
    </code>
    </div>
class: p5.Camera
params:
  - name: angle
    description: |
      <p>वर्तमान में घुमाने के लिए राशि <a href="#/p5/angelMode">angleMode()</a>।</p>
    type: Number
chainable: false
---


# tilt
