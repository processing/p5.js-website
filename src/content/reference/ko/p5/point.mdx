---
title: point
module: 도형
submodule: 2D 기초 조형
file: src/core/shape/2d_primitives.js
description: >
  <p>공간에 하나의 점(point)을 그립니다.</p>

  <p>점의 기본 너비는 하나의 픽셀입니다. 점에 색상을 입히려면

  <a href="/reference/p5/stroke">stroke()</a> 함수를 사용하세요.
  너비를 변경하려면

  <a href="/reference/p5/strokeWeight">strokeWeight()</a> 함수를 사용하세요. 

  점은 채울 수 없으므로, <a href="/reference/p5/fill">fill()</a> 함수가

  점의 색상에 영향을 주지 않습니다.</p>

  <p><code>point()</code> 함수의 하나의 매개변수를 사용하는
  버전은

  해당 점의 위치를 x- 및 y- 좌표로 설정할 수 있으며, 다음과 같이 사용됩니다. <code>point(10, 20)</code>.</p>

  <p><code>point()</code> 함수의 세 개의 매개변수를 사용하는 버전은 
  버전은 

  x-, y-, 및 z- 좌표를 사용하여 3D 공간에 점을 그릴 수 있습니다. 다음과 같이 사용됩니다. <code>point(10, 20,
  30)</code>.

  이것을 하기 위해 <code>WEBGL</code> 인수를

  <a href="/reference/p5/createCanvas">createCanvas()</a>에 추가해야 합니다.</p>

  <p><code>point()</code> 함수의 하나의 매개변수를 사용하는 버전은
  점의 위치를

  <a href="/reference/p5/p5.Vector">p5.Vector</a> 객체로 설정할 수 있습니다.</p>
line: 731
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Top-left.
      point(30, 20);

      // Top-right.
      point(85, 20);

      // Bottom-right.
      point(85, 75);

      // Bottom-left.
      point(30, 75);

      describe(
        'Four small, black points drawn on a gray canvas. The points form the corners of a square.'
      );
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Top-left.
      point(30, 20);

      // Top-right.
      point(70, 20);

      // Style the next points.
      stroke('purple');
      strokeWeight(10);

      // Bottom-right.
      point(70, 80);

      // Bottom-left.
      point(30, 80);

      describe(
        'Four points drawn on a gray canvas. Two are black and two are purple. The points form the corners of a square.'
      );
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Top-left.
      let a = createVector(30, 20);
      point(a);

      // Top-right.
      let b = createVector(70, 20);
      point(b);

      // Bottom-right.
      let c = createVector(70, 80);
      point(c);

      // Bottom-left.
      let d = createVector(30, 80);
      point(d);

      describe(
        'Four small, black points drawn on a gray canvas. The points form the corners of a square.'
      );
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('Two purple points drawn on a gray canvas.');
    }

    function draw() {
      background(200);

      // Style the points.
      stroke('purple');
      strokeWeight(10);

      // Top-left.
      point(-20, -30);

      // Bottom-right.
      point(20, 30);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('Two purple points drawn on a gray canvas. The scene spins slowly.');
    }

    function draw() {
      background(200);

      // Rotate around the y-axis.
      rotateY(frameCount * 0.01);

      // Style the points.
      stroke('purple');
      strokeWeight(10);

      // Top-left.
      point(-20, -30, 0);

      // Bottom-right.
      point(20, 30, -50);
    }
    </code>
    </div>
class: p5
overloads:
  - line: 731
    params:
      - name: x
        description: |
          <p>x 좌표입니다.</p>
        type: Number
      - name: 'y'
        description: |
          <p>y 좌표입니다.</p>
        type: Number
      - name: z
        description: |
          <p>z 좌표입니다. (WebGL 모드용)</p>
        type: Number
        optional: true
    chainable: 1
  - line: 895
    params:
      - name: coordinateVector
        description: |
          <p>좌표 벡터입니다.</p>
        type: p5.Vector
    chainable: 1
chainable: true
---


# point
