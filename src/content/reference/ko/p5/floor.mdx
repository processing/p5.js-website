---
title: floor
module: 수학
submodule: 계산
file: src/math/calculation.js
description: "<p>어떤 수에 가장 가까우면서 그 수보다 작거나 같은 정수 값을 계산합니다.</p>"
line: 294
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Use RGB color with values from 0 to 1.
      colorMode(RGB, 1);

      noStroke();

      // Draw the left rectangle.
      let r = 0.8;
      fill(r, 0, 0);
      rect(0, 0, 50, 100);

      // Round r down to 0.
      r = floor(r);

      // Draw the right rectangle.
      fill(r, 0, 0);
      rect(50, 0, 50, 100);

      describe('Two rectangles. The one on the left is bright red and the one on the right is black.');
    }
    </code>
    </div>
class: p5
params:
  - name: n
    description: |2-

      <p>내림할 숫자</p>
    type: Number
return:
  description: 내림된 숫자
  type: Integer
chainable: false
---

# floor
