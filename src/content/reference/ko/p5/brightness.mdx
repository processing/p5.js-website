---
title: 밝기값
module: 색상
submodule: 만들기 & 읽기
file: src/color/creating_reading.js
description: |2-

  <p>색상값으로 부터 밝기값을 추출합니다.</p>


  <p><code>brightness()</code> a에서 HSB brightness 값을 추출합니다


  <a href="/reference/p5/p5.Color">p5.Color</a> 개체, 색상 배열
  구성요소, 또는


  CSS 컬러 스트링.</p>


  <p>기본적으로, <code>brightness()</code> 는 다음과 같이 색상의 HSB 밝기값을 반환합니다
  0의 범위


  ~100까지. <a href="/reference/colorMode">colorMode()</a> 가 HSB로 설정된 경우,
  그것을


  지정된 범위의 밝기 값을 반환합니다.</p>
line: '253'
isConstructor: 거짓(false)
itemtype: 매서드(method)
example:
 "- "\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  // HSB 색상을 사용합니다.\n\n  colorMode(HSB);\n\n\n\n  // HSB p5.Color 객체를 만듭니다.\n\n  let c = color(0, 50, 100);\n\n\n\n  // 왼쪽 직사각형을 그립니다.\n\n  noStroke();\n\n  fill(c);\n\n  rect(15, 15, 35, 70);\n\n\n\n  // 밝기값을 100으로 설정합니다. \n\n  let brightValue = brightness(c);\n\n\n\n  // 오른쪽 직사각형을 그립니다.\n\n  fill(brightValue);\n\n  rect(50, 15, 35, 70);\n\n\n\n  describe('직사각형 두 개. 왼쪽은 살몬 핑크, 오른쪽은 흰색입니다.');\n\n}\n\n</code>\n\n</div>""
 "- "\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  //  HSB 색상을 사용합니다.\n\n  colorMode(HSB);\n\n\n\n  // 색상 배열을 만듭니다. \n\n  let c = [0, 50, 100];\n\n\n\n  // 왼쪽 직사각형을 그립니다.\n\n  noStroke();\n\n  fill(c);\n\n  rect(15, 15, 35, 70);\n\n\n\n  // 밝기값('brightValue')을 100으로 설정합니다. \n\n  let brightValue = brightness(c);\n\n\n\n  // 오른쪽 직사각형을 그립니다.\n\n  fill(brightValue);\n\n  rect(50, 15, 35, 70);\n\n\n\n  describe('직사각형 두 개. 왼쪽은 살몬 핑크, 오른쪽은 흰색입니다.');\n\n}\n\n</code>\n\n</div>""
 "- "\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  // HSB 색상을 사용합니다.\n\n  colorMode(HSB);\n\n\n\n  // CSS 색상 문자열을 만듭니다.\n\n  let c = 'rgb(255, 128, 128)';\n\n\n\n  // 왼쪽 직사각형을 그립니다.\n\n  noStroke();\n\n  fill(c);\n\n  rect(15, 15, 35, 70);\n\n\n\n  // 밝기값('brightValue')을 100으로 설정합니다. \n\n  let brightValue = brightness(c);\n\n\n\n  // 오른쪽 직사각형을 그립니다.\n\n  fill(brightValue);\n\n  rect(50, 15, 35, 70);\n\n\n\n  describe('직사각형 두 개. 왼쪽은 살몬 핑크, 오른쪽은 흰색입니다.');\n\n}\n\n</code>\n\n</div>""
 "- "\n\n\n<div>\n\n<code>\n\nfunction setup() {\n\n  createCanvas(100, 100);\n\n\n\n  background(200);\n\n\n\n  // 값의 범위가 0-255인 HSB 색상을 사용합니다.\n\n  colorMode(HSB, 255);\n\n\n\n  // p5.Color 개체를 만듭니다.\n\n  let c = color(0, 127, 255);\n\n\n\n  // 왼쪽 직사각형을 그립니다.\n\n  noStroke();\n\n  fill(c);\n\n  rect(15, 15, 35, 70);\n\n\n\n  // 밝기값('brightValue')을 255로 설정합니다. \n\n\n  let brightValue = brightness(c);\n\n\n\n  // 오른쪽 직사각형을 그립니다.\n\n  fill(brightValue);\n\n  rect(50, 15, 35, 70);\n\n\n\n  describe('직사각형 두 개. 왼쪽은 살몬 핑크, 오른쪽은 흰색입니다.');\n\n}\n\n</code>\n\n</div>""
class: p5
params:
  - name: color
  description: |2-

    <p><a href="#/p5.Color">p5.Color</a>객체, 배열

                                            색상 구성 요소, CSS 색상 문자열.</p>
  type: p5.Color|Number[]|String
return:
  description: 밝기값
  type: 숫자(Number)
chainable: 거짓(false)
---

# 밝기값(brightness)
