---
title: DEGREES
module: 상수
submodule: 상수
file: src/core/constants.js
description: >
  <p><code>String</code> 상수로, <a href="/reference/p5/angleMode">angleMode()</a>를 설정하는 데 사용됩니다.</p>
  
  <p>기본적으로 <a href="/reference/p5/rotate">rotate()</a> 및 <a href="/reference/p5/sin">sin()</a>과 같은 함수는 라디안(radian) 단위로 측정된 각도를 요구합니다.
  
  <code>angleMode(DEGREES)</code>를 호출하면 각도가 도 단위로 측정됩니다.</p>
  
  <p>참고: <code>TWO_PI</code> 라디안은 360˚와 같습니다.</p>
line: 547
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Draw a red arc from 0 to HALF_PI radians.
      fill(255, 0, 0);
      arc(50, 50, 80, 80, 0, HALF_PI);

      // Use degrees.
      angleMode(DEGREES);

      // Draw a blue arc from 90˚ to 180˚.
      fill(0, 0, 255);
      arc(50, 50, 80, 80, 90, 180);

      describe('The bottom half of a circle drawn on a gray background. The bottom-right quarter is red. The bottom-left quarter is blue.');
    }
    </code>
    </div>
class: p5
type: String
---


# DEGREES
