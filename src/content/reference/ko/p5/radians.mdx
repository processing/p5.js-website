
---
title: degrees
module: 수학
submodule: 삼각법
file: src/math/trigonometry.js
description: |
  <p>라디안으로 측정된 각도를 도 단위의 값으로 반환합니다.</p>
  <p>도와 라디안은 모두 각도를 측정하는 단위입니다. 한 바퀴 회전은 360˚입니다. 한 바퀴 회전은 2 × π (약 6.28) 라디안입니다.</p>
  <p>같은 각도를 두 단위 중 어느 하나로 표현할 수 있습니다. 예를 들어, 90°는 한 회전의 4분의 1입니다. 90°는 2 × π ÷ 4 (약 1.57) 라디안입니다.</p>
line: 479
isConstructor: false
itemtype: 메소드
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate the angle conversion.
      let rad = QUARTER_PI;
      let deg = degrees(rad);

      // Display the conversion.
      text(`${round(rad, 2)} rad = ${deg}˚`, 10, 50);

      describe('The text "0.79 rad = 45˚".');
    }
    </code>
    </div>
class: p5
params:
  - name: radians
    description: |
      <p> 도 단위로 바꾸려는 라디안 값</p>
    type: Number
return:
  description: 변환된 각도
  type: Number(숫자)
chainable: false
---


# degrees
