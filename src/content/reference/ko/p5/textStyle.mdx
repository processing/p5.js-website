---
title: textStyle
module: 타이포그래피
submodule: 속성
file: src/typography/attributes.js
description: >
  <p><a href="/reference/p5/text">text()</a>를 호출할 때의 시스템 글꼴의 스타일을 설정합니다.</p>
  
  <p>매개변수 <code>style</code>은 <code>NORMAL</code>, <code>ITALIC</code>,
  
  <code>BOLD</code>, <code>BOLDITALIC</code> 중 하나입니다.</p>
  
  <p><code>textStyle()</code>은 CSS 스타일링에 의해 덮어씌워질 수 있습니다.
  
  이 함수는 <a href="/reference/p5/loadFont">loadFont()</a>로 불러온 글꼴에는 영향을 주지 않습니다.</p>
line: 197
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Style the text.
      textSize(12);
      textAlign(CENTER);

      // First row.
      textStyle(NORMAL);
      text('Normal', 50, 15);

      // Second row.
      textStyle(ITALIC);
      text('Italic', 50, 40);

      // Third row.
      textStyle(BOLD);
      text('Bold', 50, 65);

      // Fourth row.
      textStyle(BOLDITALIC);
      text('Bold Italic', 50, 90);

      describe('The words "Normal" displayed normally, "Italic" in italic, "Bold" in bold, and "Bold Italic" in bold italics.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 197
    params:
      - name: style
        description: |
          <p>글꼴 스타일, ITALIC, BOLD, BOLDITALIC 중 하나.</p>
        type: Constant
    chainable: 1
  - line: 244
    params: []
    return:
      description: ''
      type: String
chainable: true
---


# textStyle
