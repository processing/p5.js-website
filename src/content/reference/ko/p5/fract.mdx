---
title: fract
module: 수학
submodule: 계산
file: src/math/calculation.js
description: >
  <p>어떤 숫자의 분수 부분을 계산합니다.</p>

  <p>어떤 숫자의 분수 부분은 소수점을 포함합니다. 예를 들어, <code>fract(12.34)</code>는 0.34를 반환합니다.</p>
line: 1048
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Original number.
      let n = 56.78;
      text(n, 50, 33);

      // Fractional part.
      let f = fract(n);
      text(f, 50, 67);

      describe('The number 56.78 written above the number 0.78.');
    }
    </code>
    </div>
class: p5
params:
  - name: n
    description: |
      <p>분수 부분을 찾을 수 있는 숫자</p>
    type: Number
return:
  description: n의 분수 부분
  type: Number
chainable: false
---

# fract
