---
title: noTint
module: 이미지
submodule: 불러오기 & 보이기
file: src/image/loading_displaying.js
description: >
  <p><a href="/reference/p5/tint"> tint()</a>로 설정된 현재 틴트를 제거합니다.</p>

  <p><code>noTint()</code>는 이미지를 원래의 색상으로 복구합니다.<p>
line: 1345
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // Load the image.
    function preload() {
      img = loadImage('assets/laDefense.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // Left.
      // Tint with a CSS color string.
      tint('red');
      image(img, 0, 0);

      // Right.
      // Remove the tint.
      noTint();
      image(img, 50, 0);

      describe('Two images of an umbrella and a ceiling side-by-side. The image on the left has a red tint.');
    }
    </code>
    </div>
class: p5
chainable: false
---


# noTint
