---
title: round
module: 수학
submodule: 계산
file: src/math/calculation.js
description: >
  <p>어떤 숫자에 가장 가까운 정수를 계산합니다.</p>

  <p>예를 들어, <code>round(133.8)</code>는 134라는 값을 반환합니다.</p>

  <p>두번째 매개변수인 <code>decimals</code>은 선택 사항입니다. 반올림할 때 사용할 소수점의 자리 수를 설정합니다. 예를 들어, <code>round(12.34, 1)</code>은 12.3을 반환합니다. <code>decimals</code>의 기본값은 입니다.</p>
line: 861
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Round a number.
      let x = round(4.2);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display the rounded number.
      text(x, 50, 50);

      describe('The number 4 written in middle of the canvas.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Round a number to 2 decimal places.
      let x = round(12.782383, 2);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display the rounded number.
      text(x, 50, 50);

      describe('The number 12.78 written in middle of canvas.');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    type: Number
    description: |
      <p>반올림할 숫자</p>
  - name: decimals
    type: Number
    optional: true
    description: |
      <p>반올림할 소수점 자리 수 (기본값 0)</p>
return:
  description: 반올림된 숫자
  type: Integer
chainable: false
---

# round
