---
title: play
module: p5.sound
submodule: SoundFile
file: src/sources/SoundFile.js
description: Start the soundfile. Same as the start() method.
line: 161
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    async function setup() {
     sample = await loadSound("/assets/chime.mp3");
     createCanvas(100, 100);
     textAlign(CENTER);
     textWrap(WORD);
     textSize(8);
     describe("a sketch that plays a soundfile");
    }

    function draw() {
      background(220);
      text("click to play the sound, demonstrating the start() method", 0, 20, width);
    }

    function mousePressed() {
      sample.start();
    }
    </code>
    </div>
class: p5.SoundFile
chainable: false
---


# play
