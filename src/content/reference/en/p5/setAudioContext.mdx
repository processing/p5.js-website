---
title: setAudioContext
module: p5.sound
submodule: p5.sound
file: src/core/Utils.js
description: >-
  Sets the audio context to a specified context to enable cross library
  compatibility.


  Accepts an AudioContext, an OfflineAudioContext, or a Tone.js Context.

  A raw AudioContext supplied here gets the same Firefox fixes
line: 105
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let osc, ctx

    function setup() {
      createCanvas(400, 400);
      //route p5.sound.js through an audio context the sketch owns
      ctx = new AudioContext();
      setAudioContext(ctx);
      //nodes created from here on play through that context
      osc = new p5.Oscillator();
    }

    function draw() {
      background(220);
    }

    function mousePressed() {
      osc.start();
    }
    </code>
    </div>
class: p5
params:
  - name: context
    description: the desired audio context.
    type: AudioContext|OfflineAudioContext|Context
chainable: false
---


# setAudioContext
