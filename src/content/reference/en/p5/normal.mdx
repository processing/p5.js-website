---
title: normal
module: Shape
submodule: Vertex
file: src/core/shape/vertex.js
description: >
  <p>Sets the 3d vertex normal to use for subsequent vertices drawn with

  <a href="/reference/p5/vertex">vertex()</a>. A normal is a vector that is
  generally

  nearly perpendicular to a shape's surface which controls how much light will

  be reflected from that part of the surface.</p>
line: 1107
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);
      noStroke();
    }

    function draw() {
      background(255);
      rotateY(frameCount / 100);
      normalMaterial();
      beginShape(TRIANGLE_STRIP);
      normal(-0.4, 0.4, 0.8);
      vertex(-30, 30, 0);

      normal(0, 0, 1);
      vertex(-30, -30, 30);
      vertex(30, 30, 30);

      normal(0.4, -0.4, 0.8);
      vertex(30, -30, 0);
      endShape();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1107
    params:
      - name: vector
        description: |
          <p>A p5.Vector representing the vertex normal.</p>
        type: Vector
    chainable: 1
  - line: 1144
    params:
      - name: x
        description: |
          <p>The x component of the vertex normal.</p>
        type: Number
      - name: 'y'
        description: |
          <p>The y component of the vertex normal.</p>
        type: Number
      - name: z
        description: |
          <p>The z component of the vertex normal.</p>
        type: Number
    chainable: 1
chainable: true
---


# normal
