---
title: uniformStorage
module: 3D
submodule: p5.strands
file: src/strands/p5.strands.js
description: >
  <p>Declares a storage buffer uniform inside a <a
  href="/reference/p5.Shader/modify/">modify()</a> callback,

  making a <a href="/reference/p5/createStorage/">createStorage()</a> buffer
  accessible in the shader.</p>

  <p>Pass a <code>p5.StorageBuffer</code> (or a function returning one) as the
  second argument

  to set it as the default value, applied automatically each frame. Pass a plain

  object with the same field layout as the buffer's struct elements to declare
  the

  schema without binding a specific buffer.</p>

  <p>When called without a name, p5.strands automatically uses the name of the

  variable it is assigned to as the uniform name.</p>

  <p>Note: <code>uniformStorage</code> is only available when using
  p5.strands.</p>
line: 212
isConstructor: false
itemtype: method
example: []
class: p5
beta: true
webgpu: true
webgpuOnly: true
return:
  description: A strands node representing the storage buffer.
overloads:
  - params:
      - name: name
        description: The name of the storage buffer uniform in the shader.
        type: String
      - name: bufferOrSchema
        description: >-
          A storage buffer to bind,

          a function returning a storage buffer (called each frame), or a plain
          object

          describing the struct field layout.
        optional: 1
        type: p5.StorageBuffer|Function|Object
    return:
      description: A strands node representing the storage buffer.
  - params:
      - name: bufferOrSchema
        description: ''
        optional: 1
        type: p5.StorageBuffer|Function|Object
    return:
      description: ''
chainable: false
---


# uniformStorage
