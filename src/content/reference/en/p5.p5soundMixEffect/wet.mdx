---
title: wet
module: p5.sound
submodule: p5.sound
file: src/core/p5soundMixEffect.js
description: >-
  Adjusts the balance between the source node's original (dry) or effected (wet)
  signal.
line: 22
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let osc, del

    function setup() {
      createCanvas(100, 100);
      osc = new p5.Oscillator();
      osc.disconnect();
      del = new p5.Delay();
      osc.connect(del);
      del.delayTime(0.25);
      del.feedback(0.5);
      //changes the blend between the original and effect signal to 50%
      del.wet(0.5);
    }

    function mousePressed() {
      osc.start();
    }

    function draw() {
      del.delayTime(map(mouseX, 0, width, 0.01, 0.5));
      //map the blend between effect and dry signal to mouse position
      del.wet(map(mouseY, 0, height, 0.1, 0.9));
      background(220);
      textAlign(CENTER);
      textSize(9);
      text('delay dry/wet: ' + del.wet().toFixed(2), width / 2, height / 2);
    }
    </code>
    </div>
class: p5.p5soundMixEffect
params:
  - name: amount
    description: a value between 0 (dry) and 1 (wet).
    type: Number
chainable: false
---


# wet
