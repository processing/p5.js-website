---
title: column
module: Math
submodule: ''
file: src/math/Matrices/Matrix.js
description: >
  <p>A function that returns a column vector of a NxN matrix.</p>

  <p>This method extracts a specific column from the matrix and returns it as a
  <code>p5.Vector</code>.

  The column is determined by the <code>rowIndex</code> parameter, which
  specifies the row index

  of the matrix. This is useful for operations that require working with
  individual

  columns of a matrix, such as column transformations or dot products.</p>
line: 467
isConstructor: false
itemtype: method
example:
  - |-
    // Extracting a column vector from a 3x3 matrix
    const matrix = new p5.Matrix([1, 2, 3, 4, 5, 6, 7, 8, 9]);
    const columnVector = matrix.column(1); // Returns a vector [4, 5, 6]

    // p5.js script example
    <div class="norender"><code>
    function setup() {

      const matrix = new p5.Matrix([1, 2, 3, 4, 5, 6, 7, 8, 9]);
      const columnVector = matrix.column(1); // Extract the second column (index 1)
      console.log("Column Vector:", columnVector.toString()); // Output: Column Vector: [4, 5, 6]
    }
    </code></div>
class: p5.Matrix
return:
  description: A <code>p5.Vector</code> representing the extracted column of the matrix.
  type: p5.Vector
overloads:
  - params:
      - name: rowIndex
        description: |-
          The index of the row to extract as a column vector.
          Must be a non-negative integer less than the matrix dimension.
        type: Number
    return:
      description: >-
        A <code>p5.Vector</code> representing the extracted column of the
        matrix.
      type: p5.Vector
chainable: false
---


# column
