---
title: p5.Panner
module: p5.sound
submodule: p5.sound
file: src/effects/Panner.js
description: A panning effect.
line: 11
isConstructor: true
params:
  - name: panAmount
    description: >-
      defaults to 0, accepts values between -1 and 1 representing the pan
      position of the sound source between the left and right speaker channels.
    type: Number
    optional: true
example:
  - |-

    <div>
    <code>
    let panner, lfo, soundfile, cnv;

    function preload() {
      soundfile = loadSound('/assets/beat.mp3');
    }

    function setup() {
      createCanvas(100, 100);
      describe("a sketch that pans a sound source to the left speaker channel")
      background(220);
      panner = new p5.Panner(-1);
      soundfile.loop();
      soundfile.disconnect();
      soundfile.connect(panner);
    }

    function mousePressed() {
      soundfile.start();
    }
    </code>
    </div>
methods:
  pan:
    description: Pan a sound source left or right.
    path: p5.Panner/pan
chainable: false
---


# p5.Panner
