---
title: fract
module: 数学
submodule: 计算
file: src/math/calculation.js
description: |2-

  <p>计算一个数字的小数部分。</p>
  <p>一个数字的小数部分包括其小数值。例如，
  <code>fract(12.34)</code> 返回 0.34。</p>
line: '1048'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Original number.
      let n = 56.78;
      text(n, 50, 33);

      // Fractional part.
      let f = fract(n);
      text(f, 50, 67);

      describe('The number 56.78 written above the number 0.78.');
    }
    </code>
    </div>
class: p5
params:
  - name: n
    description: |2-
  
      <p>将被计算的数字的小数部分。</p>
    type: 数字
return:
  description: n的小数部分。
  type: 数字
chainable: false
---

# fract
