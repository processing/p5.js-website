---
title: noDebugMode
module: 3D
submodule: 交互
file: src/webgl/interaction.js
description: >
  <p>在 3D 绘图中关闭 <a href="/reference/p5/debugMode">debugMode()</a>。</p>
line: 725
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。
    function setup() {
      createCanvas(100, 100, WEBGL);

      // 启用调试模式。
      debugMode();

      describe('A multicolor box on a gray background. A grid and axes icon are displayed near the box. They disappear when the user double-clicks.');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 设定盒子样式。
      normalMaterial();

      // 绘制盒子。  box(20, 40);
    }

    // 当用户双击时禁用调试模式。
    function doubleClicked() {
      noDebugMode();
    }
    </code>
    </div>
class: p5
chainable: false
---


# noDebugMode
