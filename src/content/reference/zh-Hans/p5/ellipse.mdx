---
title: ellipse
module: 形状
submodule: 2D 基础形状
file: src/core/shape/2d_primitives.js
description: |2-

  <p>绘制一个椭圆。</p>


  <p>椭圆是一个由 <code>x</code>、<code>y</code>、
  <code>w</code> 和 <code>h</code>  参数设置的圆形。 <code>x</code> 和 <code>y</code> 设置其中心点。 <code>w</code> 和 <code>h</code> 设置其宽度和高度。移步 <a href="/reference/p5/ellipseMode">ellipseMode()</a> 查看其他设置椭圆位置的方式。</p>


  <p>如果未设置高度，则椭圆的宽度和高度均使用宽度值。如果指定了为负数的高度或宽度，则其绝对值将被采用。</p>


  <p>第五个参数<code>detail</code>也是可选的。它决定了有多少顶点会被用于在 WebGL 模式下绘制椭圆。默认值为 25。</p>
line: '373'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      ellipse(50, 50, 80, 80);

      describe('A white circle on a gray canvas.');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      ellipse(50, 50, 80);

      describe('A white circle on a gray canvas.');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      ellipse(50, 50, 80, 40);

      describe('A white ellipse on a gray canvas.');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      ellipse(0, 0, 80, 40);

      describe('A white ellipse on a gray canvas.');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      // Use 6 vertices.
      ellipse(0, 0, 80, 40, 6);

      describe('A white hexagon on a gray canvas.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: '373'
  params:
    - name: x
    description: |2-

      <p>椭圆中心的 x 坐标。</p>
    type: 数字
    - name: y
    description: |2-

      <p>椭圆中心的 y 坐标。</p>
    type: 数字
    - name: w
    description: |2-

      <p>椭圆的宽度。</p>
    type: 数字
    - name: h
    description: |2-

      <p>椭圆的高度。</p>
    type: 数字
    optional: true
  chainable: '1'
  - line: '474'
  params:
    - name: x
    description: ''
    type: 数字
    - name: y
    description: ''
    type: Number
    - name: w
    description: ''
    type: 数字
    - name: h
    description: ''
    type: 数字
    - name: detail
    description: |2-

      <p>仅适用于 WebGL 模式的可选参数。这是为了

                              指定组成顶点的数量

                              椭圆的周长。默认值为 25。不会

                              对于超过 50 个的细节进行描边。</p>
    type: 整数
    optional: true
chainable: true
---

# ellipse
