---
title: p5.Color
module: 颜色
submodule: 创建和读取
file: src/color/p5.Color.js
description: >
  <p>这是一个用于描述颜色的类。</p>

  <p>每个 <code>p5.Color</code> 对象都保存了其被创建时所采用的颜色模式和最大值限制。这些信息被用来解释传入对象构造函数的参数，并决定了如调用 <a href="/reference/p5/saturation">saturation()</a> 函数时的输出格式。</p>

  <p>颜色在内部以浮点形式的理想 RGBA 值数组存储，从 0 到 1 归一化。这些值用于计算最接近的屏幕颜色，即从 0 到 255 的 RGBA 级别。这些显示颜色随后被输出到渲染器。</p>

  <p>当计算不同的颜色表示时，结果会被缓存以提高性能。这些值是归一化的浮点数。</p>

  <p>注：推荐使用 <a href="/reference/p5/color">color()</a> 方法来创建这个类的实例。</p>
line: 318
isConstructor: true
params:
  - name: pInst
    description: |
      <p>指向 p5 实例的指针。</p>
    type: P5
    optional: true
  - name: vals
    description: |
      <p>包含红色、绿色、蓝色和透明度通道的颜色值或 CSS 颜色的数组。</p>
    type: '数字[]|字符串'
methods:
  toString:
    description: >
      <p>返回以 <code>String</code> 为格式的颜色。</p>

      <p>调用 <code>myColor.toString()</code> 在调试时很有用，例如 <code>print(myColor.toString())</code>。这对于将 p5.js 与其他库一起使用也很有帮助。</p>

      <p>参数 <code>format</code> 是可选的。如果传入了格式字符串，比如 <code>myColor.toString('#rrggbb')</code>，它将决定颜色字符串的格式。默认情况下，颜色字符串的格式为 <code>'rgba(r, g, b, a)'</code>。</p>
    path: p5.Color/toString
  setRed:
    description: >
      <p>设置颜色的红色分量。</p>

      <p>范围取决于 <a href="/reference/colorMode">colorMode()</a>。在默认的 RGB 模式下，它的值介于 0 到 255 之间。</p>
    path: p5.Color/setRed
  setGreen:
    description: >
      <p>设置颜色的绿色分量。</p>

      <p>范围取决于 <a href="/reference/colorMode">colorMode()</a>。在默认的 RGB 模式下，它的值介于 0 到 255 之间。</p>
    path: p5.Color/setGreen
  setBlue:
    description: >
      <p>设置颜色的蓝色分量。</p>

      <p>范围取决于 <a href="/reference/colorMode">colorMode()</a>。在默认的 RGB 模式下，它的值介于 0 到 255 之间。</p>
    path: p5.Color/setBlue
  setAlpha:
    description: >
      <p>设置颜色的 alpha 值（透明度）。</p>

      <p>范围取决于 <a href="/reference/colorMode">colorMode()</a>。在默认的 RGB 模式下，它的值介于 0 到 255 之间。</p>
    path: p5.Color/setAlpha
chainable: false
---


# p5.Color
