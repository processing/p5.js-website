---
title: floor
module: 数学
submodule: 计算
file: src/math/calculation.js
description: |2-

  <p>计算最接近给定数字且小于或等于它的整数值。</p>
line: '294'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Use RGB color with values from 0 to 1.
      colorMode(RGB, 1);

      noStroke();

      // Draw the left rectangle.
      let r = 0.8;
      fill(r, 0, 0);
      rect(0, 0, 50, 100);

      // Round r down to 0.
      r = floor(r);

      // Draw the right rectangle.
      fill(r, 0, 0);
      rect(50, 0, 50, 100);

      describe('Two rectangles. The one on the left is bright red and the one on the right is black.');
    }
    </code>
    </div>
class: p5
params:
  - name: n
  description: |2-

    <p>需要向下取整的数字。</p>
  type: 数字
return:
  description: 向下取整后的数字。
  type: 整数
chainable: 'false'
---

# floor
