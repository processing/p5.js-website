---
title: panorama
module: 3D
submodule: Lights
file: src/webgl/light.js
description: >
  <p>创建一个沉浸式的 3D 背景。</p>

  <p><code>panorama()</code> 将包含 360˚ 内容的图像，例如地图或 HDRIs，转换为沉浸式的 3D 背景，围绕在绘图周围。探索空间需要通过诸如
　<a href="/reference/p5/orbitControl">orbitControl()</a> 或 <a href="/reference/p5/camera">camera()</a> 等函数来改变相机的视
　角。</p>
line: 1020
isConstructor: false
itemtype: method
example:
  - |-

    <div class="notest">
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。
    let img;

    // 加载图像并创建一个 p5.Image 对象。
    function preload() {
      img = loadImage('/assets/outdoor_spheremap.jpg');
    }

    function setup() {
      createCanvas(100 ,100 ,WEBGL);

      describe('一个悬浮在环境上方的球体。球体的表面反射着环境。用户拖动鼠标时，可以以 3D 方式查看整个环境。');
    }

    function draw() {
      // 添加全景图。
      panorama(img);

      // 启用鼠标轨道控制。
      orbitControl();

      // 使用图像作为光源。
      imageLight(img);

      // 为球体添加样式。
      noStroke();
      specularMaterial(50);
      shininess(200);
      metalness(100);

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>
class: p5
params:
  - name: img
    description: |
      <p>用作背景的360˚图像。</p>
    type: p5.Image
chainable: false
---


# panorama
