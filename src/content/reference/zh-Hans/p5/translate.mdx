---
title: translate
module: Transform
submodule: Transform
file: src/core/transform.js
description: >
  <p>指定在显示窗口内显示对象的量。</p>
  <p>x 参数指定左/右移动，y 参数指定上/下移动。</p>
  <p>变换是累积的，适用于之后发生的所有事情，并且对函数的连续调用会有累积效果。例如，调用 translate(50, 0)，然后再调用 translate(20, 0) 等同于调用 translate(70, 0)。如果在 <a href="/reference/p5/draw">draw()</a> 中调用 <a href="/reference/p5/translate">translate()</a>，则当循环重新开始时，变换会被重置。可以通过使用 a href="/reference/p5/push">push()</a> 和 <a href="/reference/p5/pop">pop()</a> 进一步控制此函数。</p>
line: 529
isConstructor: false
itemtype: method
alt: |-
  white 55×55 rect with black outline at center right.
  3 white 55×55 rects with black outlines at top-l, center-r and bottom-r.
  a 20×20 white rect moving in a circle around the canvas
example:
  - |-

    <div>
    <code>
    translate(30, 20);
    rect(0, 0, 55, 55);
    </code>
    </div>

    <div>
    <code>
    rect(0, 0, 55, 55); // Draw rect at original 0,0
    translate(30, 20);
    rect(0, 0, 55, 55); // Draw rect at new 0,0
    translate(14, 14);
    rect(0, 0, 55, 55); // Draw rect at new 0,0
    </code>
    </div>


    <div>
    <code>
    function draw() {
      background(200);
      rectMode(CENTER);
      translate(width / 2, height / 2);
      translate(p5.Vector.fromAngle(millis() / 1000, 40));
      rect(0, 0, 20, 20);
    }
    </code>
    </div>
class: p5
overloads:
  - line: 529
    params:
      - name: x
        description: |
          <p>左/右移动。</p>
        type: Number
      - name: 'y'
        description: |
          <p>上/下移动</p>
        type: Number
      - name: z
        description: |
          <p>前进/后退移动（仅适用于WebGL）。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 582
    params:
      - name: vector
        description: |
          <p>要进行移动的向量。</p>
        type: p5.Vector
    chainable: 1
chainable: true
---


# translate
