---
title: ambientLight
module: 3D
submodule: Lights
file: src/webgl/light.js
description: >
  <p>创建一个从所有方向发光的灯光。</p>

  <p>环境光不是来自一个方向。相反，3D形状从所有方向均匀地受到照亮。环境光几乎总是与其他类型的光一起使用。</p>

  <p>有三种调用 <code>ambientLight()</code> 的方式，可以通过可选参数设置光的颜色。</p>

  <p>第一种调用 <code>ambientLight()</code> 的方式有两个参数，
  <code>gray</code> 和

  <code>alpha</code>。 <code>alpha</code> 是可选的。 可以传递介于 0 和 255 之间的灰度和 alpha 值来设置环境光的颜色， 例如 <code>ambientLight(50)</code> 或 <code>ambientLight(50, 30)</code>。</p>

 <p>第二种调用 <code>ambientLight()</code> 的方式有一个参数，颜色。可以传递一个 <a href="/reference/p5/p5.Color">p5.Color</a> 对象、一个颜色值数组或一个 CSS 颜色字符串，例如 <code>ambientLight('magenta')</code>，来设置环境光的颜色。</p>

<p>第三种调用 <code>ambientLight()</code> 的方式有四个参数，
  <code>v1</code>、 <code>v2</code>、 <code>v3</code> 和 <code>alpha</code>。 <code>alpha</code> 是可选的。可以传递 RGBA、HSBA 或 HSLA 值来设置环境光的颜色，例如 <code>ambientLight(255, 0, 0)</code> 或 <code>ambientLight(255, 0, 0, 30)</code>。颜色值将根据当前的 <a href="/reference/p5/colorMode">colorMode()</a> 进行解释。</p>
line: 10
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。
    // 双击画布以打开灯光。

    let isLit = false;

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个绘制在灰色背景上的球体。当用户双击时，球体会改变其颜色。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 控制灯光。
      if (isLit === true) {
        // 使用灰度值为 80。
        ambientLight(80);
      }

      // 绘制球体。
      sphere(30);
    }

    // 当用户双击时打开环境光。
    function doubleClicked() {
      isLit = true;
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制了一个褪色的品红色球体。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开灯光。
      // 使用一个 p5.Color 对象。
      let c = color('orchid');
      ambientLight(c);

      // 绘制球体。
      sphere();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个绘制在灰色背景上的褪色的品红色球体。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开灯光。
      // 使用一个 CSS 颜色字符串。
      ambientLight('#DA70D6');

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个绘制在灰色背景上的褪色的品红色球体。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开灯光。
      // 使用 RGB 值。
      ambientLight(218, 112, 214);

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>
class: p5
overloads:
  - line: 10
    params:
      - name: v1
        description: |
          <p>当前<a href="#/p5/colorMode">colorMode()</a>中的红色或色相值。</p>
        type: Number
      - name: v2
        description: |
          <p>当前<a href="#/p5/colorMode">colorMode()</a>中的绿色或饱和度值。</p>
        type: Number
      - name: v3
        description: |
          <p>当前<a href="#/p5/colorMode">colorMode()</a>中的蓝色、亮度或明度值。</p>
        type: Number
      - name: alpha
        description: |
          <p>当前<a href="#/p5/colorMode">colorMode()</a>中的 alpha（透明度）值。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 164
    params:
      - name: gray
        description: |
          <p>介于 0 和 255 之间的灰度值。</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 171
    params:
      - name: value
        description: |
          <p>作为 CSS 字符串的颜色。</p>
        type: String
    chainable: 1
  - line: 177
    params:
      - name: values
        description: |
          <p>作为 RGBA、HSBA 或 HSLA 值数组的颜色。</p>
        type: 'Number[]'
    chainable: 1
  - line: 184
    params:
      - name: color
        description: |
          <p>作为 <a href="#/p5.Color">p5.Color</a> 对象的颜色。</p>
        type: p5.Color
    chainable: 1
chainable: true
---


# ambientLight
