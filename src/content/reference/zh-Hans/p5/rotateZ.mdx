---
title: rotateZ
module: Transform
submodule: Transform
file: src/core/transform.js
description: >
  <p>通过角度参数指定的量围绕 Z 轴旋转形状。可以通过 RADIANS 或 DEGREES 输入角度。</p>
  <p>此方法仅在 WebGL 模式下有效。</p>
  <p>对象始终围绕它们相对于原点的位置旋转，正数会使对象顺时针旋转。</p>
  <p>当 <a href="/reference/p5/draw">draw()</a> 重新开始时，所有变换都会被重置。</p>
line: 337
isConstructor: false
itemtype: method
alt: 3d box rotating around the z axis.
example:
  - |-

    <div modernizr='webgl'>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);
      camera(0, 0, 50*sqrt(3), 0, 0, 0, 0, 1, 0);
      perspective(PI/3, 1, 5*sqrt(3), 500*sqrt(3));
    }
    function draw() {
      background(255);
      rotateZ(millis() / 1000);
      box();
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: |
      <p>旋转角度，根据当前的 angleMode，指定为弧度或度数。</p>
    type: Number
chainable: true
---


# rotateZ
