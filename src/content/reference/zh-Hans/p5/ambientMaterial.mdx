---
title: ambientMaterial
module: 3D
submodule: Material
file: src/webgl/material.js
description: >
  <p>设置形状表面材质的环境颜色。</p>

  <p><code>ambientMaterial()</code> 颜色设置形状将反射的 <a href="/reference/p5/ambientLight">ambientLight()</a> 颜色的组件。例如，调用 <code>ambientMaterial(255, 255, 0)</code> 将使形状反射红色和绿色光，但不反射蓝色光。</p>

  <p><code>ambientMaterial()</code> 可以通过不同的参数以三种方式调用来设置材质的颜色。</p>

  <p>第一种调用 <code>ambientMaterial()</code> 的方式有一个参数 <code>gray</code>。

可以传递介于 0 和 255 之间的灰度值，如 <code>ambientMaterial(50)</code>，来设置材质的颜色。较高的灰度值会使形状看起来更亮。</p>

  <p>第二种调用 <code>ambientMaterial()</code> 的方式有一个参数 <code>color</code>。可以传递一个 <a href="/reference/p5/p5.Color">p5.Color</a> 对象、一个颜色值数组或一个 CSS 颜色字符串，如 <code>ambientMaterial('magenta')</code>，来设置材质的颜色。</p>

  <p>第三种调用 <code>ambientMaterial()</code> 的方式有三个参数 <code>v1</code>、<code>v2</code> 和 <code>v3</code>。可以传递 RGB、HSB 或 HSL 值，如 <code>ambientMaterial(255, 0, 0)</code>，来设置材质的颜色。颜色值将根据当前的 <a href="/reference/p5/colorMode">colorMode()</a> 进行解释。</p>

  <p>注意：<code>ambientMaterial()</code> 只能在 WebGL 模式下使用。</p>
line: 1561
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个品红色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个品红色的环境光。
      ambientLight(255, 0, 255);

      // 绘制立方体。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个紫色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个品红色的环境光。
      ambientLight(255, 0, 255);

      // 添加一个深灰色的环境材质。
      ambientMaterial(150);

      // 绘制立方体。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个红色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个品红色的环境光。
      ambientLight(255, 0, 255);

      // 使用 RGB 值添加一个黄色的环境材质。
      ambientMaterial(255, 255, 0);

      // 绘制立方体。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个红色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个品红色的环境光。
      ambientLight(255, 0, 255);

      // 使用 p5.Color 对象添加一个黄色的环境材质。
      let c = color(255, 255, 0);
      ambientMaterial(c);

      // 绘制立方体。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个红色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个品红色的环境光。
      ambientLight(255, 0, 255);

      // 使用颜色字符串添加一个黄色的环境材质。
      ambientMaterial('yellow');

      // 绘制立方体。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('一个黄色的立方体绘制在灰色背景上。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 打开一个白色的环境光。
      ambientLight(255, 255, 255);

      // 使用颜色字符串添加一个黄色的环境材质。
      ambientMaterial('yellow');

      // 绘制立方体。
      box();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1561
    params:
      - name: v1
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的红色或色调值。</p>
        type: Number
      - name: v2
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的绿色或饱和度值。</p>
        type: Number
      - name: v3
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的蓝色、亮度或明度值。</p>
        type: Number
    chainable: 1
  - line: 1766
    params:
      - name: gray
        description: |
          <p>介于 0 （黑色）和 255 （白色）之间的灰度值。</p>
        type: Number
    chainable: 1
  - line: 1772
    params:
      - name: color
        description: |
          <p>作为 <a href="#/p5.Color">p5.Color</a> 对象、颜色值数组或 CSS 字符串的颜色。</p>
        type: 'p5.Color|Number[]|String'
    chainable: 1
chainable: true
---


# ambientMaterial
