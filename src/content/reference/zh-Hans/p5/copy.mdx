---
title: copy
module: 图像
submodule: 像素
file: src/image/pixels.js
description: |2-

  <p>将像素从源图像复制到画布的某个区域。</p>


  <p>第一个参数 <code>srcImage</code> 是 <a href="/reference/p5/p5.Image">p5.Image</a> 要混合的对象。源图像可以是画布本身或是一个 <a href="/reference/p5/p5.Image">p5.Image</a> 对象。如果源区域与目标区域的像素大小不同，<code>copy()</code> 可以缩放像素。</p>

  <p>接下来的四个参数，<code>sx</code>，<code>sy</code>，
  <code>sw</code> 和 <code>sh</code> 确定源图像的复制区域。 <code>(sx, sy)</code> 是区域左上角。 <code>sw</code> 和 <code>sh</code> 是区域宽度和高度。</p>


  <p>接下来的四个参数，<code>dx</code>，<code>dy</code>，
  <code>dw</code> 和 <code>dh</code> 确定复制到画布的区域。<code>(dx, dy)</code> 是区域左上角。 <code>dw</code> 和 <code>dh</code> 是区域宽度和高度。</p>
line: '288'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // Load the image.
    function preload() {
      img = loadImage('/assets/rockies.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // Use the mountains as the background.
      background(img);

      // Copy a region of pixels to another spot.
      copy(img, 7, 22, 10, 10, 35, 25, 50, 50);

      // Outline the copied region.
      stroke(255);
      noFill();
      square(7, 22, 10);

      describe('An image of a mountain landscape. A square region is outlined in white. A larger square contains a pixelated view of the outlined region.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: '288'
    params:
      - name: srcImage
        description: |2-
    
          <p>源图像。</p>
        type: p5.Image|p5.Element
      - name: sx
        description: |2-
    
          <p>源左上角的 x 坐标。</p>
        type: 整数
      - name: sy
        description: |2-
    
          <p>源左上角的 y 坐标。</p>
        type: 整数
      - name: sw
        description: |2-
    
          <p>源图像宽度。</p>
        type: 整数
      - name: sh
        description: |2-
    
          <p>源图像高度。</p>
        type: 整数
      - name: dx
        description: |2-
    
          <p>目标左上角的 x 坐标。</p>
        type: 整数
      - name: dy
        description: |2-
    
          <p>目标左上角的 y 坐标。</p>
        type: 整数
      - name: dw
        description: |2-
    
          <p>目标图像宽度。</p>
        type: 整数
      - name: dh
        description: |2-
    
          <p>目标图像高度。</p>
        type: 整数
  - line: '345'
    params:
      - name: sx
        description: ''
        type: 整数
      - name: sy
        description: ''
        type: 整数
      - name: sw
        description: ''
        type: 整数
      - name: sh
        description: ''
        type: 整数
      - name: dx
        description: ''
        type: 整数
      - name: dy
        description: ''
        type: 整数
      - name: dw
        description: ''
        type: 整数
      - name: dh
        description: ''
        type: 整数
chainable: false
---

# copy
