---
title: width
module: 环境
submodule: 环境
file: src/core/environment.js
description: |2-

  <p><code>Number</code> 变量存储画布宽度，单位是像素。</p>


  <p><code>width</code> 的默认值是 0，调用 <a href="./createCanvas">createCanvas()</a> 或
  <a href="./resizeCanvas">resizeCanvas()</a> 会改变 <code>width</code> 的值。 调用 <a href="./noCanvas">noCanvas()</a> 将其值设为 0。</p>
line: '803'
isConstructor: 'false'
itemtype: property
example:
  - |2-



  <div>

  <code>

  function setup() {

 "background(200);"



 "// Display the canvas' width."

 "text(width, 42, 54);"



 "describe('The number 100 written in black on a gray square.');"

  }

  </code>

  </div>



  <div>

  <code>

  function setup() {

 "createCanvas(50, 100);"



 "background(200);"



 "// Display the canvas' width."

 "text(width, 21, 54);"



 "describe('The number 50 written in black on a gray rectangle.');"

  }

  </code>

  </div>



  <div>

  <code>

  function setup() {

 "createCanvas(100, 100);"



 "background(200);"



 "// Display the canvas' width."

 "text(width, 42, 54);"



 "describe('The number 100 written in black on a gray square. When the mouse is pressed, the square becomes a rectangle and the number becomes 50.');"

  }



  // If the mouse is pressed, reisze

  // the canvas and display its new

  // width.

  function mousePressed() {

    if (mouseX > 0 && mouseX < width && mouseY > 0 && mouseY < height) {

 "resizeCanvas(50, 100);"

 "background(200);"

 "text(width, 21, 54);"

    }

  }

  </code>

  </div>
class: p5
type: Number
---

# width
