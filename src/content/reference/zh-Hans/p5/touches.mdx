---
title: touches
module: Events
submodule: Touch
file: src/events/touch.js
description: |
  <p>系统变量 touches[] 包含一个数组，其中包含所有当前点击点相对于画布（0, 0）的位置，以及标识一个特定点击点移动的 ID。数组中的每个元素都是一个具有 x、y 和 id 属性的对象。</p>
  <p>在触屏台式机（笔记本电脑）上，touches[] 数组不支持 Safari 和 IE 浏览器。</p>
line: 10
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // On a touchscreen device, touch
    // the canvas using one or more fingers
    // at the same time
    function draw() {
      clear();
      let display = touches.length + ' touches';
      text(display, 5, 10);
      describe(`Number of touches currently registered are displayed
        on the canvas`);
    }
    </code>
    </div>
class: p5
type: 'Object[]'
---


# touches
