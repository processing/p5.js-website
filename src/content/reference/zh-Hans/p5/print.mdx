---
title: print
module: 环境
submodule: 环境
file: src/core/environment.js
description: |2-

  <p>在网络浏览器的控制台中显示文本。</p>


  <p>在调试过程中，<code>print()</code> 可以帮助我们打印值。每次调用 <code>print()</code> 都会创建一行新的文本。</p>


  <p>注意: "调用 <code>print('\n')</code> 可打印空行。不带参数的情况下，调用 <code>print()</code> 会打开浏览器的打印文档对话框。</p>"
line: '21'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code class="norender">
    function setup() {
      // Prints "hello, world" to the console.
      print('hello, world');
    }
    </code>
    </div>

    <div>
    <code class="norender">
    function setup() {
      let name = 'ada';
      // Prints "hello, ada" to the console.
      print(`hello, ${name}`);
    }
    </code>
    </div>
class: p5
params:
  - name: contents
    description: |2-
  
      <p>显示在控制台的内容</p>
    type: 任何类型
chainable: false
---

# print
