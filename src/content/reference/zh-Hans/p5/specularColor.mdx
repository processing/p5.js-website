---
title: specularColor
module: 3D
submodule: Lights
file: src/webgl/light.js
description: >
  <p>设置灯光的高光颜色。</p>

  <p><code>specularColor()</code> 影响那些在表面上以首选方向反射的光线。

  这些光线包括

  <a href="/reference/p5/directionalLight">directionalLight()</a>,

  <a href="/reference/p5/pointLight">pointLight()</a>, 和

  <a href="/reference/p5/spotLight">spotLight()</a>。 该函数有助于在使用

  <a href="/reference/p5/specularMaterial">specularMaterial()</a> 样式化的

  <a href="/reference/p5/p5.Geometry">p5.Geometry</a> 对象上创建高光。

  如果几何体不使用

  <a href="/reference/p5/specularMaterial">specularMaterial()</a>，那么
  <code>specularColor()</code> 将不起作用。</p>

  <p>注意： <code>specularColor()</code> 不影响所有方向反射的光线，

  包括 <a href="/reference/p5/ambientLight">ambientLight()</a>
  和

  <a href="/reference/p5/imageLight">imageLight()</a>。</p>

  <p>有三种调用 <code>specularColor()</code> 的方式，可以通过可选参数设置高光颜色。</p>

  <p>第一种调用 <code>specularColor()</code> 有两个可选参数， <code>gray</code>

  和 <code>alpha</code>。 介于 0 和 255 之间的灰度和 alpha 值， 例如

  <code>specularColor(50)</code> 或 <code>specularColor(50, 80)</code>, 可以传递来设置高光颜色。</p>

  <p>第二种调用 <code>specularColor()</code> 有一个可选参数，

  <code>color</code>。 可以传递一个 <a href="/reference/p5/p5.Color">p5.Color</a> 对象, 一个颜色值数组， 或一个 CSS 颜色字符串来 
  设置高光颜色。</p>

  <p>调用 <code>specularColor()</code> 的第三种方式具有四个可选参数， <code>v1</code>, <code>v2</code>, <code>v3</code>, 和 
  <code>alpha</code>。 RGBA, HSBA, 或 HSLA 值，例如 <code>specularColor(255, 0, 0, 80)</code>, 可用于设置高光颜色。颜色值将使用 
  当前的 <a href="/reference/p5/colorMode">colorMode()</a> 进行解释。</p>
line: 205
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制了一个白色的球体。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 无镜面颜色。
      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。
    // 双击画布以添加一个点光源。

    let isLit = false;

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制了一个球体。当用户双击时，聚光灯开始发光。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 给球体设置样式。
      noStroke();
      specularColor(100);
      specularMaterial(255, 255, 255);

      // 控制灯光。
      if (isLit === true) {
        // 从右上方添加一个白色点光源。
        pointLight(255, 255, 255, 30, -20, 40);
      }

      // 绘制球体。
      sphere(30);
    }

    // 当用户双击时打开点光源。
    function doubleClicked() {
      isLit = true;
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制一个黑色的球体。球体表面的一个区域以蓝色突出显示。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 添加一个镜面高光。
      // 使用一个 p5.Color 对象。
      let c = color('dodgerblue');
      specularColor(c);

      // 从右上方添加一个白色点光源。
      pointLight(255, 255, 255, 30, -20, 40);

      // 为球体设置样式。
      noStroke();

      // 添加白色的镜面材质。
      specularMaterial(255, 255, 255);

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制了一个黑色的球体。球体表面的一个区域以蓝色突出显示。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 添加一个镜面高光。
      // 使用 CSS 颜色字符串。
      specularColor('#1E90FF');

      // 从右上方添加一个白色点光源。
      pointLight(255, 255, 255, 30, -20, 40);

      // 为球体设置样式。
      noStroke();

      // 添加白色的镜面材质。
      specularMaterial(255, 255, 255);

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // 单击并拖动鼠标以从不同角度查看场景。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('在灰色背景上绘制了一个黑色的球体。球体表面的一个区域以蓝色突出显示。');
    }

    function draw() {
      background(200);

      // 使用鼠标进行轨道控制。
      orbitControl();

      // 添加一个镜面高光。
      // 使用 RGB 值。
      specularColor(30, 144, 255);

      // 从右上方添加一个白色点光源。
      pointLight(255, 255, 255, 30, -20, 40);

      // 为球体设置样式。
      noStroke();

      // 添加一个白色的高光材质。
      specularMaterial(255, 255, 255);

      // 绘制球体。
      sphere(30);
    }
    </code>
    </div>
class: p5
overloads:
  - line: 205
    params:
      - name: v1
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的红色或色相值。</p>
        type: Number
      - name: v2
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的绿色或饱和度值。</p>
        type: Number
      - name: v3
        description: |
          <p>当前 <a href="#/p5/colorMode">colorMode()</a> 中的蓝色、亮度或明度值。</p>
        type: Number
    chainable: 1
  - line: 423
    params:
      - name: gray
        description: |
          <p>介于 0 和 255 之间的灰度值。</p>
        type: Number
    chainable: 1
  - line: 429
    params:
      - name: value
        description: |
          <p>作为 CSS 字符串的颜色。</p>
        type: String
    chainable: 1
  - line: 435
    params:
      - name: values
        description: |
          <p>作为 RGBA、HSBA 或 HSLA 值数组的颜色。</p>
        type: 'Number[]'
    chainable: 1
  - line: 442
    params:
      - name: color
        description: |
          <p>作为 <a href="#/p5.Color">p5.Color</a> 对象的颜色。</p>
        type: p5.Color
    chainable: 1
chainable: true
---


# specularColor
