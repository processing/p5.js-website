---
title: norm
module: 数学
submodule: 计算
file: src/math/calculation.js
description: |2-

  <p>将一个数字从一个范围映射到 0 到 1 之间的值。</p>
  <p>例如，<code>norm(2, 0, 10)</code> 返回 0.2。数字 2 在原始范围 [0, 10] 中的位置与 0.2 在范围 [0, 1] 中的位置成比例。这与调用 <code>map(2, 0, 10, 0, 1)</code> 是相同的。</p>
  <p>超出原始范围的数字不会被限制在 0 和 1 之间。超出范围的值通常是有意的并且很有用。</p>
line: '772'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Use RGB color with values from 0 to 1.
      colorMode(RGB, 1);

      describe('A square changes color from black to red as the mouse moves from left to right.');
    }

    function draw() {
      // Calculate the redValue.
      let redValue = norm(mouseX, 0, 100);

      // Paint the background.
      background(redValue, 0, 0);
    }
    </code>
    </div>
class: p5
params:
  - name: value
  description: |2-

    <p>要进行归一化的输入值。</p>
  type: 数字
  - name: start
  description: |2-

    <p>数值当前范围的下界。</p>
  type: 数字
  - name: stop
  description: |2-

    <p>数值当前范围的上界。</p>
  type: 数字
return:
  description: 归一化后的数值。
  type: 数字
chainable: false
---

# norm
