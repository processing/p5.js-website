---
title: move
module: 3D
submodule: Camera
file: src/webgl/p5.Camera.js
description: >
  <p>Mueve la cámara a lo largo de sus ejes locales (en el espacio "local") sin cambiar su orientación.</p>

  <p>Los parámetros <code>x</code>, <code>y</code> y <code>z</code> son las
  distancias que la cámara debe

  mover. Por ejemplo, llamar a <code>myCamera.move(10, 20, 30)</code> mueve
  la cámara 10

  pixeles a la derecha, 20 pixeles hacia abajo y 30 pixeles hacia atrás en su

  espacio local.</p>
line: 2923
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // Click the canvas to begin detecting key presses.

    let cam;

    function setup() {
      createCanvas(100, 100, WEBGL);

      // Create the first camera.
      // Keep its default settings.
      cam = createCamera();

      // Place the camera at the top-right.
      cam.setPosition(400, -400, 800);

      // Point it at the origin.
      cam.lookAt(0, 0, 0);

      describe(
        'A white cube drawn against a gray background. The cube appears to move when the user presses certain keys.'
      );
    }

    function draw() {
      background(200);

      // Move the camera along its "local" axes
      // when the user presses certain keys.
      if (keyIsPressed === true) {

        // Move horizontally.
        if (keyCode === LEFT_ARROW) {
          cam.move(-1, 0, 0);
        }
        if (keyCode === RIGHT_ARROW) {
          cam.move(1, 0, 0);
        }

        // Move vertically.
        if (keyCode === UP_ARROW) {
          cam.move(0, -1, 0);
        }
        if (keyCode === DOWN_ARROW) {
          cam.move(0, 1, 0);
        }

        // Move in/out of the screen.
        if (key === 'i') {
          cam.move(0, 0, -1);
        }
        if (key === 'o') {
          cam.move(0, 0, 1);
        }
      }

      // Draw the box.
      box();
    }
    </code>
    </div>
class: p5.Camera
params:
  - name: x
    description:
      <p>distancia para moverse a lo largo del eje "local" x del cámara.</p>
    type: Number
  - name: 'y'
    description:
      <p>distancia para moverse a lo largo del eje "local" y del cámara.</p>
    type: Number
  - name: z
    description:
      <p>distancia para moverse a lo largo del eje "local" z del cámara.</p>
    type: Number
chainable: false
---


# move
