---
title: fract
module: Matemáticas
submodule: Cálculos
file: src/math/calculation.js
description: |2-

  <p>Calcula la parte fraccionaria de un número.</p>

  <p>La parte fraccionaria de un número incluye sus valores decimales. Por ejemplo,

  <code>fract(12.34)</code> devuelve 0.34.</p>
line: '1048'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Original number.
      let n = 56.78;
      text(n, 50, 33);

      // Fractional part.
      let f = fract(n);
      text(f, 50, 67);

      describe('The number 56.78 written above the number 0.78.');
    }
    </code>
    </div>
class: p5
params:
  - name: n
    description: |2-
  
      <p>el número cuya parte fraccionaria será encontrada.</p>
    type: Número
return:
  description: parte fraccionaria de n.
  type: Número
chainable: 'false'
---

# fract
