---
title: textLeading
module: Tipografía
submodule: Atributos
file: src/typography/attributes.js
description: >
  <p>Establece el espaciado entre líneas de texto cuando se llama a <a href="/reference/p5/text">text()</a>.</p>

  <p>Nota: El espaciado se mide en píxeles.</p>

  <p>Llamar a <code>textLeading()</code> sin argumentos devuelve el espaciado actual.</p>
line: 107
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // "\n" starts a new line of text.
      let lines = 'one\ntwo';

      // Left.
      text(lines, 10, 25);

      // Right.
      textLeading(30);
      text(lines, 70, 25);

      describe('Las palabras "one" y "two" escritas en líneas separadas dos veces. Las palabras a la izquierda tienen menos espacio vertical que las palabras a la derecha.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 107
    params:
      - name: leading
        description: |
          <p>espaciado entre líneas de texto en unidades de píxeles.</p>
        type: Number
    chainable: 1
  - line: 142
    params: []
    return:
      description: ''
      type: Number
chainable: true
---


# textLeading
