---
title: fill
module: Color
submodule: Configuración
file: src/color/setting.js
description: >
  <p>Establece el color que se va a utilizar para rellenar las formas.</p>
  <p>Llamar a <code>fill(255, 165, 0)</code> o <code>fill('orange')</code> significa que todas las formas dibujadas después del comando de relleno estarán rellenas con el color naranja.</p>
  <p>La versión de <code>fill()</code> con un parámetro interpreta el valor de tres maneras. Si el parámetro es un <code>Number</code> (número), se interpreta como un valor de escala de grises. Si el parámetro es un <code>String</code> (cadena de caracteres), se interpreta como una cadena de color CSS  (colores predefinidos de CSS). También se puede proporcionar un objeto <a href="/reference/p5/p5.Color">p5.Color</a> para establecer el color de relleno.</p>
  <p>La versión de <code>fill()</code> con tres parámetros los interpreta como colores RGB, HSB o HSL, dependiendo del modo de color actual (<a href="/reference/p5/colorMode">colorMode()</a>). El espacio de color predeterminado es RGB, con cada valor en el rango de 0 a 255.</p>
line: 982
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // A grayscale value.
      fill(51);
      square(20, 20, 60);

      describe('A dark charcoal gray square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // R, G & B values.
      fill(255, 204, 0);
      square(20, 20, 60);

      describe('A yellow square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(100);

      // Use HSB color.
      colorMode(HSB);

      // H, S & B values.
      fill(255, 204, 100);
      square(20, 20, 60);

      describe('A royal blue square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // A CSS named color.
      fill('red');
      square(20, 20, 60);

      describe('A red square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Three-digit hex RGB notation.
      fill('#fae');
      square(20, 20, 60);

      describe('A pink square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Six-digit hex RGB notation.
      fill('#A251FA');
      square(20, 20, 60);

      describe('A purple square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Integer RGB notation.
      fill('rgb(0, 255, 0)');
      square(20, 20, 60);

      describe('A bright green square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Integer RGBA notation.
      fill('rgba(0, 255, 0, 0.25)');
      square(20, 20, 60);

      describe('A soft green rectange with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Percentage RGB notation.
      fill('rgb(100%, 0%, 10%)');
      square(20, 20, 60);

      describe('A red square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Percentage RGBA notation.
      fill('rgba(100%, 0%, 100%, 0.5)');
      square(20, 20, 60);

      describe('A dark fuchsia square with a black outline.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // A p5.Color object.
      let c = color(0, 0, 255);
      fill(c);
      square(20, 20, 60);

      describe('A blue square with a black outline.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 982
    params:
      - name: v1
        description: >
          <p>el valor del color rojo en el modo de color RGB, o el valor del matiz si el modo de color es HSB. </p>
        type: Number
      - name: v2
        description: >
          <p>el valor del color verde en el modo de color RGB, o el valor de la saturación si el modo de color es HSB. </p>
        type: Number
      - name: v3
        description: >
          <p>el valor del color azul en el modo de color RGB, o el valor del brillo si el modo de color es HSB. </p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 1187
    params:
      - name: value
        description: |
          <p>cadena de caracteres que definen un color.</p>
        type: String
    chainable: 1
  - line: 1193
    params:
      - name: gray
        description: |
          <p>especifica un valor entre blanco y negro (escala de grises).</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 1200
    params:
      - name: values
        description: |
          <p>Un arreglo que contenga los componentes rojo, verde, azul y alfa del color.</p>
        type: 'Number[]'
    chainable: 1
  - line: 1207
    params:
      - name: color
        description: |
          <p>el color de relleno.</p>
        type: p5.Color
    chainable: 1
chainable: true
---


# fill
