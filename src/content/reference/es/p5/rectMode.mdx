---
title: rectMode
module: Forma
submodule: Atributos
file: src/core/shape/attributes.js
description: "\n<p>Cambios donde se dibujan rectángulos y cuadrados.</p>\n\n\n<p>De forma predeterminada, los dos primeros parámetros de\n\n\n<a href=\"/reference/p5/rect\">rect()</a> y <a\nhref=\"/reference/p5/square\">square()</a>,\n\n\nson las coordenadas x-, y las coordenadas y- del centro de la forma. Los siguientes parámetros establecen\n\n\nel ancho y alto de la forma. Esto es lo mismo que llamar\n\n\n<code>rectMode(CORNER)</code>.</p>\n\n\n<p><code>rectMode(CORNERS)</code> también utiliza los dos primeros parámetros como\nubicación de\n\n\nuna de las esquinas. Los siguientes parámetros son la ubicación de la esquina opuesta.\n\n\nEste modo solo funciona para <a href=\"/reference/p5/rect\">rect()</a>.</p>\n\n\n<p><code>rectMode(CENTER)</code> utiliza los dos primeros parámetros como las coordenadas x-, y \n\n\nlas coordenadas y- del centro de la forma. Los siguientes parámetros son su ancho y\n\n\naltura.</p>\n\n\n<p><code>rectMode(RADIUS)</code> también utiliza los dos primeros parámetros como las coordenadas x-\ny\n\n\nlas coordenadas y- del centro de la forma. Los siguientes parámetros son\n\n\nla mitad del ancho y alto de la forma.</p>\n\n\n<p>El argumento pasado a <code>rectMode()</code> debe escribirse en MAYÚSCULAS\nporque las\n\n\nconstantes <code>CENTER</code>, <code>RADIUS</code>, <code>CORNER</code> y\n<code>CORNERS</code> se definen de esta manera.\n\n\nJavaScript es un lenguaje que distingue entre mayúsculas y minúsculas.</p>"
line: '184'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rectMode(CORNER);
      fill(255);
      rect(25, 25, 50, 50);

      rectMode(CORNERS);
      fill(100);
      rect(25, 25, 50, 50);

      describe('A small gray square drawn at the top-left corner of a white square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rectMode(RADIUS);
      fill(255);
      rect(50, 50, 30, 30);

      rectMode(CENTER);
      fill(100);
      rect(50, 50, 30, 30);

      describe('A small gray square drawn at the center of a white square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rectMode(CORNER);
      fill(255);
      square(25, 25, 50);

      describe('A white square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rectMode(RADIUS);
      fill(255);
      square(50, 50, 30);

      rectMode(CENTER);
      fill(100);
      square(50, 50, 30);

      describe('A small gray square drawn at the center of a white square.');
    }
    </code>
    </div>
class: p5
params:
  - name: mode
    description: |2-
  
      <p>ya sea CORNER, CORNERS, CENTER, o RADIUS</p>
    type: Constant
chainable: true
---

# rectMode
