---
title: strokeCap
module: Forma
submodule: Atributos
file: src/core/shape/attributes.js
description: |2-

  <p>Establece el estilo para representar los finales de las líneas.</p>


  <p>Las tapas de los finales de línea son redondeadas (<code>ROUND</code>), cuadradas


  (<code>SQUARE</code>), o extendidas (<code>PROJECT</code>). El límite predeterminado es
  <código>REDONDO</código>.</p>


  <p>El argumento pasado a <code>strokeCap()</code> debe escribirse TODO EN MAYÚSCULAS
  porque


  las constantes <code>ROUND</code>, <code>SQUARE</code> y
  <code>PROJECT</code> se definen de esta manera.


  JavaScript es un lenguaje que distingue entre mayúsculas y minúsculas.</p>
line: '387'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      strokeWeight(12);

      // Top.
      strokeCap(ROUND);
      line(20, 30, 80, 30);

      // Middle.
      strokeCap(SQUARE);
      line(20, 50, 80, 50);

      // Bottom.
      strokeCap(PROJECT);
      line(20, 70, 80, 70);

      describe(
        'Three horizontal lines. The top line has rounded ends, the middle line has squared ends, and the bottom line has longer, squared ends.'
      );
    }
    </code>
    </div>
class: p5
params:
  - name: cap
  description: |2-

    <p>ya sea ROUND, SQUARE, o PROJECT</p>
  type: Constant
chainable: 'true'
---

# strokeCap
