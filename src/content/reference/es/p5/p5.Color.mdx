---
title: p5.Color
module: Color
submodule: Creación y Lectura
file: src/color/p5.Color.js
description: >
  <p>Una clase para describir un color.</p>

  <p>Cada objeto <code>p5.Color</code> almacena el modo de color

  y los máximos de nivel que estaban activos durante su construcción. Estos valores se

  utilizan para interpretar los argumentos pasados al constructor del objeto. También

  determinan el formato de salida, como cuando

  <a href="/reference/p5/saturation">saturation()</a> es llamado.</p>

  <p>El color se almacena internamente como un arreglo de valores RGBA ideales en punto flotante

  normalizado de 0 a 1. Estos valores se utilizan para calcular los colores de pantalla más cercanos, que son niveles RGBA de 0 a 255. Los colores de pantalla

  se envían al renderizador.</p>

  <p>Cuando se calculan diferentes representaciones de color, los resultados se almacenan en caché

  para mejorar el rendimiento. Estos valores son números en punto flotante normalizados.</p>

  <p>Nota: <a href="/reference/p5/color">color()</a> es la forma recomendada de

  crear una instancia de esta clase.</p>
line: 318
isConstructor: true
params:
  - name: pInst
    description: |
      <p>puntero a la instancia de p5.</p>
    type: P5
    optional: true
  - name: vals
    description: |
      <p>un arreglo que contiene los valores de color
                                               para los canales rojo, verde, azul y alfa
                                               o el color CSS.</p>
    type: 'Number[]|String'
methods:
  toString:
    description: >
      <p>Devuelve el color formateado como una <code>String</code>.</p>

      <p>Llamar a <code>myColor.toString()</code> puede ser útil para depurar, como en

      <code>print(myColor.toString())</code>. También es útil para usar p5.js

      con otras bibliotecas.</p>

      <p>El parámetro <code>format</code> es opcional. Si se pasa una cadena de caracteres con el formato, como en

      <code>myColor.toString('#rrggbb')</code>, determinará cómo se formatea la cadena de color.

      Por defecto, las cadenas de color se formatean como <code>'rgba(r, g, b, a)'</code>.</p>
    path: p5.Color/toString
  setRed:
    description: >
      <p>Establece el componente rojo de un color.</p>

      <p>El rango depende del <a href="/reference/colorMode">colorMode()</a>.

      En el modo RGB predeterminado está entre 0 y 255.</p>
    path: p5.Color/setRed
  setGreen:
    description: >
      <p>Establece el componente verde de un color.</p>

      <p>El rango depende del <a href="/reference/colorMode">colorMode()</a>. En el

      modo RGB predeterminado está entre 0 y 255.</p>
    path: p5.Color/setGreen
  setBlue:
    description: >
      <p>Establece el componente azul de un color.</p>

      <p>El rango depende del <a href="/reference/colorMode">colorMode()</a>. En el

      modo RGB predeterminado está entre 0 y 255.</p>
    path: p5.Color/setBlue
  setAlpha:
    description: >
      <p>Establece el valor alfa (transparencia) de un color.</p>

      <p>El rango depende del <a href="/reference/colorMode">colorMode()</a>.

      En el modo RGB predeterminado está entre 0 y 255.</p>
    path: p5.Color/setAlpha
chainable: false
---


# p5.Color
