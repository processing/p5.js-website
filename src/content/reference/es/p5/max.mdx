---
title: max
module: Matemáticas
submodule: Cálculos
file: src/math/calculation.js
description: |-
  <p><code>max()</code> determina el valor más  grande dentro de una secuencia de números. </p>
  <p>La versión de <code>max()</code> con un parámetro, interpreta el parámetro como un arreglo de números y devuelve el número más grande. Por ejemplo, si llamamos max(numbers), el argumento <code>numbers</code> debe ser un arreglo de números.</p>
  <p>La versión  de <code>max()</code> con dos o mas parámetros, interpreta los parámetros como número individuales, y devuelve el número más grande. Por ejemplo, si llamamos max (10, 5,20), obtenemos 20 como resultado.</p>
line: '618'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate the maximum of 10, 5, and 20.
      let m = max(10, 5, 20);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display the max.
      text(m, 50, 50);

      describe('The number 20 written in the middle of a gray square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Create an array of numbers.
      let numbers = [10, 5, 20];

      // Calculate the maximum of the array.
      let m = max(numbers);

      // Style the text.
      textAlign(CENTER);
      textSize(16);

      // Display the max.
      text(m, 50, 50);

      describe('The number 20 written in the middle of a gray square.');
    }
    </code>
    </div>
class: p5
return:
  description: número más grande.
  type: Number
overloads:
  - line: '618'
  params:
    - name: n0
    description: |2-

      <p>primer número a comparar.</p>
    type: Number
    - name: n1
    description: |2-

      <p>segundo número a comparar.</p>
    type: Number
  return:
    description: número más grande.
    type: Number
  - line: '680'
  params:
    - name: nums
    description: |2-

      <p>números a comparar.</p>
    type: Number[]
  return:
    description: ''
    type: Number
chainable: 'false'
---

# max
