---
title: windowWidth
module: Ambiente
submodule: Ambiente
file: src/core/environment.js
description: |-
  <p>La variable <code>windowWidth</code> almacena el ancho de la ventana del navegador.</p>
  <p>Por otro lado, la ventana gráfica de diseño (conocida como <a href="https://developer.mozilla.org/en-US/docs/Glossary/Layout_viewport" target="_blank">layout viewport</a>) es el espacio dentro del navegador que está disponible para dibujar."</p>
line: '652'
isConstructor: 'false'
itemtype: property
alt: Este ejemplo no renderiza nada.
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // Set the canvas' width and height
      // using the browser's dimensions.
      createCanvas(windowWidth, windowHeight);

      background(200);

      describe('A gray canvas that takes up the entire browser window.');
    }
    </code>
    </div>
class: p5
type: Number
---

# windowWidth
