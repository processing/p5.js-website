---
title: asin
module: Matemáticas
submodule: Trigonometría
file: src/math/trigonometry.js
description: "\n<p>Calcula el arco seno de un número.</p>\n\n\n<p><code>asin()</code> es el inverso de <a\nhref=\"/reference/p5/sin\">sin()</a>. \n\nEspera valores de entrada en el rango de -1 a 1. Por defecto, <code>asin()</code> devuelve valores en el\nrango -π ÷ 2 (aproximadamente -1,57) a π ÷ 2 (aproximadamente 1,57). \n\nSi el <a href=\"/reference/p5/angleMode\">angleMode()</a> es <code>DEGREES</code>, devuelve valores en el rango -90 a 90.</p>"
line: '80'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate sin() and asin() values.
      let a = PI / 3;
      let s = sin(a);
      let as = asin(s);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(s, 3)}`, 35, 50);
      text(`${round(as, 3)}`, 35, 75);

      describe('The numbers 1.047, 0.866, and 1.047 written on separate rows.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate sin() and asin() values.
      let a = PI + PI / 3;
      let s = sin(a);
      let as = asin(s);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(s, 3)}`, 35, 50);
      text(`${round(as, 3)}`, 35, 75);

      describe('The numbers 4.189, -0.866, and -1.047 written on separate rows.');
    }
    </code>
    </div>
class: p5
params:
  - name: value
  description: |2-

    <p>valor cuyo arco seno se va a calcular.</p>
  type: Número
return:
  description: arco seno del valor dado.
  type: Número
chainable: 'false'
---

# asin
