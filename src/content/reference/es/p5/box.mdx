---
title: box
module: Shape
submodule: 3D Primitives
file: src/webgl/3d_primitives.js
description: >
  <p>Dibuja un cubo (prisma rectangular).</p>

  <p>Un cubo es una forma tridimensional con seis caras. Cada cara hace un ángulo de 90 grados con cuatro caras vecinas.</p>

  <p>El primer parámetro, <code>ancho</code>, es opcional. Si se pasa un <code>Number</code>, como en <code>box(20)</code>, establece el ancho y alto del cubo. Por defecto, <code>ancho</code> es 50.</p>

  <p>El segundo parámetro, <code>alto</code>, también es opcional. Si se pasa un <code>Number</code>, como en <code>box(20, 30)</code>, establece el alto del cubo. Por defecto, <code>alto</code> se establece al ancho del cubo.</p>

  <p>El tercer parámetro, <code>profundidad</code>, también es opcional. Si se pasa un <code>Number</code>, como en <code>box(20, 30, 40)</code>, establece la profundidad del cubo. Por defecto, <code>profundidad</code> se establece al alto del cubo.</p>

  <p>El cuarto parámetro, <code>detailX</code>, también es opcional. Si se pasa un <code>Number</code>, como en <code>box(20, 30, 40, 5)</code>, establece el número de subdivisiones triangulares para usar en el eje X. Todas las formas tridimensionales se construyen conectando triángulos para formar sus superficies. Por defecto, <code>detailX</code> es 1.</p>

  <p>El quinto parámetro, <code>detailY</code>, también es opcional. Si se pasa un <code>Number</code>, como en <code>box(20, 30, 40, 5, 7)</code>, establece el número de subdivisiones triangulares para usar en el eje Y. Todas las formas tridimensionales se construyen conectando triángulos para formar sus superficies. Por defecto, <code>detailY</code> es 1.</p>

  <p>Nota: <code>box()</code> solo puede ser usado en modo WebGL.</p>
line: 1009
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // Click and drag the mouse to view the scene from different angles.

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('A white box on a gray background.');
    }

    function draw() {
      background(200);

      // Enable orbiting with the mouse.
      orbitControl();

      // Draw the box.
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // Click and drag the mouse to view the scene from different angles.

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('A white box on a gray background.');
    }

    function draw() {
      background(200);

      // Enable orbiting with the mouse.
      orbitControl();

      // Draw the box.
      // Set its width and height to 30.
      box(30);
    }
    </code>
    </div>

    <div>
    <code>
    // Click and drag the mouse to view the scene from different angles.

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('A white box on a gray background.');
    }

    function draw() {
      background(200);

      // Enable orbiting with the mouse.
      orbitControl();

      // Draw the box.
      // Set its width to 30 and height to 50.
      box(30, 50);
    }
    </code>
    </div>

    <div>
    <code>
    // Click and drag the mouse to view the scene from different angles.

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('A white box on a gray background.');
    }

    function draw() {
      background(200);

      // Enable orbiting with the mouse.
      orbitControl();

      // Draw the box.
      // Set its width to 30, height to 50, and depth to 10.
      box(30, 50, 10);
    }
    </code>
    </div>
class: p5
params:
  - name: width
    description: |
      <p>ancho de la caja.</p>
    type: Number
    optional: true
  - name: height
    description: |
      <p>altura de la caja.</p>
    type: Number
    optional: true
  - name: depth
    description: |
      <p>profundidad de la caja.</p>
    type: Number
    optional: true
  - name: detailX
    description: |
      <p>número de subdivisiones de triángulo a lo largo del eje x.</p>
    type: Integer
    optional: true
  - name: detailY
    description: |
      <p>número de subdivisiones de triángulo a lo largo del eje y.</p>
    type: Integer
    optional: true
chainable: true
---


# box
