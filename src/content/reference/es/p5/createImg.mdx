---
title: createImg
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>Crea un elemento <code><img></code> que puede aparecer fuera del 
  lienzo.</p>

  <p>El primer parámetro, <code>src</code>, es una cadena con la ruta al 
  archivo de imagen.

  <code>src</code> debe ser una ruta relativa, como en 
  <code>'assets/image.png'</code>, o una URL, como 
  
  en <code>'https://example.com/image.png'</code>.</p>

  <p>El segundo parámetro, <code>alt</code>, es una cadena con el 
  
   <a
  href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/alt#usage_notes"
  target="_blank">texto alternativo</a> 
  
  para la imagen. Se puede usar una cadena vacía <code>''</code> para imágenes que 
  no se muestran.</p>

  <p>El tercer parámetro, <code>crossOrigin</code>, es opcional. Es una cadena 
  que establece la 
  
  <a
  href="https://developer.mozilla.org/en-US/docs/Web/HTML/CORS_settings_attributes"
  target="_blank">propiedad crossOrigin</a> 
  
  de la imagen. Usa <code>'anonymous'</code> o <code>'use-credentials'</code> 
  para recuperar la imagen 
  
  con acceso de origen cruzado.</p>

  <p>El cuarto parámetro, <code>callback</code>, también es opcional. Establece una 
  función para 
  
  llamar después de que se cargue la imagen. La nueva imagen se pasa a la función de devolución de llamada

  como un objeto <a href="/reference/p5/p5.Element">p5.Element</a>.</p>
line: 648
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      let img = createImg(
        'https://p5js.org/assets/img/asterisk-01.png',
        'The p5.js magenta asterisk.'
      );
      img.position(0, -10);

      describe('A gray square with a magenta asterisk in its center.');
    }
    </code>
    </div>
class: p5
return:
  description: nuevo objeto <a href="#/p5.Element">p5.Element</a>.
  type: p5.Element
overloads:
  - line: 648
    params:
      - name: src
        description: |
          <p>ruta relativa o URL de la imagen.</p>
        type: String
      - name: alt
        description: |
          <p>texto alternativo para la imagen.</p>
        type: String
    return:
      description: nuevo objeto <a href="#/p5.Element">p5.Element</a>.
      type: p5.Element
  - line: 692
    params:
      - name: src
        description: ''
        type: String
      - name: alt
        description: ''
        type: String
      - name: crossOrigin
        description: |
          <p>propiedad crossOrigin a utilizar para recuperar la imagen.</p>
        type: String
        optional: true
      - name: successCallback
        description: |
          <p>función a llamar una vez que se cargue la imagen. La nueva imagen se pasará 
                                               a la función como un objeto <a href="#/p5.Element">p5.Element</a>.</p>
        type: Function
        optional: true
    return:
      description: nuevo objeto <a href="#/p5.Element">p5.Element</a>.
      type: p5.Element
chainable: false
---


# createImg