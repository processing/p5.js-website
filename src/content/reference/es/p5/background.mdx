---
title: background
module: Color
submodule: Configuración
file: src/color/setting.js
description: >
  <p>Establece el color a utilizar para el fondo del lienzo.</p>
  <p>Por defecto, el fondo es transparente. <code>background()</code> se utiliza típicamente dentro de <a href="/reference/p5/draw">draw()</a> para limpiar la ventana de visualización al principio de cada cuadro (fotograma). También se puede usar dentro de <a href="/reference/p5/setup">setup()</a> para establecer el fondo en el primer cuadro (fotograma) de la animación.</p>
  <p>La versión de <code>background()</code> con un parámetro interpreta el valor de cuatro maneras diferentes. Si el parámetro es un <code>Number</code> (número), se interpreta como un valor de escala de grises. Si el parámetro es un <code>String</code> (cadena de caracteres), se interpreta como una cadena de caracteres de color de CSS (colores predefinidos de CSS). Se admiten cadenas de colores RGB, RGBA, HSL, HSLA, hexadecimal y colores nombrados. Si el parámetro es un objeto <a href="/reference/p5/p5.Color">p5.Color</a>, se utilizará como el color de fondo. Si el parámetro es un objeto <a href="/reference/p5/p5.Image">p5.Image</a>, se utilizará como la imagen de fondo.</p>
  <p>La versión de <code>background()</code> con dos parámetros interpreta el primero como un valor de escala de grises. El segundo parámetro establece el valor alfa (transparencia).</p>
  <p>La versión de <code>background()</code> con tres parámetros los interpreta como colores RGB, HSB o HSL, dependiendo del modo de color actual (<a href="/reference/p5/colorMode">colorMode()</a>). Por defecto, los colores se especifican en valores RGB. Llamar a <code>background(255, 204, 0)</code> establece el fondo a un color amarillo brillante.</p>

line: 414
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // A grayscale value.
      background(51);

      describe('A canvas with a dark charcoal gray background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // A grayscale value and an alpha value.
      background(51, 0.4);
      describe('A canvas with a transparent gray background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // R, G & B values.
      background(255, 204, 0);

      describe('A canvas with a yellow background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Use HSB color.
      colorMode(HSB);

      // H, S & B values.
      background(255, 204, 100);

      describe('A canvas with a royal blue background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // A CSS named color.
      background('red');

      describe('A canvas with a red background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Three-digit hex RGB notation.
      background('#fae');

      describe('A canvas with a pink background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Six-digit hex RGB notation.
      background('#222222');

      describe('A canvas with a black background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Integer RGB notation.
      background('rgb(0, 255, 0)');

      describe('A canvas with a bright green background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Integer RGBA notation.
      background('rgba(0, 255, 0, 0.25)');

      describe('A canvas with a transparent green background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Percentage RGB notation.
      background('rgb(100%, 0%, 10%)');

      describe('A canvas with a red background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // Percentage RGBA notation.
      background('rgba(100%, 0%, 100%, 0.5)');

      describe('A canvas with a transparent purple background.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // A p5.Color object.
      let c = color(0, 0, 255);
      background(c);

      describe('A canvas with a blue background.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 414
    params:
      - name: color
        description: >
          <p>cualquier valor creado con la función<a href="#/p5/color">color()</a></p>
        type: p5.Color
    chainable: 1
  - line: 608
    params:
      - name: colorstring
        description: |
          <p>cadena de caracteres que definen un color. Los formatos posibles incluye: número rgb() o rgba(), porcentaje rgb() o rgba(), hexadecimal definidos por 3 dígitos o 6 dígitos. </p>
        type: String
      - name: a
        description: |
          <p>opacidad del fondo relativo al rango de color actual (el rango predefinido es 0 -255).</p>
        type: Number
        optional: true
    chainable: 1
  - line: 618
    params:
      - name: gray
        description: |
          <p>especifica un valor entre blanco y negro (escala de grises)</p>
        type: Number
      - name: a
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 625
    params:
      - name: v1
        description: >
          <p>el valor del color rojo en el modo de color RGB, o el valor del matiz si el modo de color es HSB.</p>
        type: Number
      - name: v2
        description: >
          <p>el valor del color verde en el modo de color RGB, o el valor de la saturación si el modo de color es HSB.</p>
        type: Number
      - name: v3
        description: >
          <p>el valor del color azul en el modo de color RGB, o el valor del brillo si el modo de color es HSB.</p>
        type: Number
      - name: a
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 634
    params:
      - name: values
        description: |
          <p>Un arreglo que contenga los componentes rojo, verde, azul y alfa del color. </p>
        type: 'Number[]'
    chainable: 1
  - line: 641
    params:
      - name: image
        description: |
          <p>p5.Image: imagen creada con <a href="#/p5/loadImage">loadImage()</a> o <a href="#/p5/createImage">createImage()</a>, para ser definida como fondo. Esta imagen debe ser del mismo tamaño que la ventana del boceto.</p>
        type: p5.Image
      - name: a
        description: ''
        type: Number
        optional: true
    chainable: 1
chainable: true
---


# background
