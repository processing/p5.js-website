---
title: copy
module: Imagen
submodule: Píxeles
file: src/image/pixels.js
description: |2-

  <p>Copia píxeles de una imagen fuente a una región del lienzo.</p>

  <p>El primer parámetro, <code>srcImage</code>, es el objeto <a href="/reference/p5/p5.Image">p5.Image</a> a mezclar. La imagen fuente puede ser el lienzo en sí mismo o un objeto <a href="/reference/p5/p5.Image">p5.Image</a>. <code>copy()</code> escalara los píxeles de la región fuente si no es del mismo tamaño que la región de destino.</p>

  <p>Los siguientes cuatro parámetros, <code>sx</code>, <code>sy</code>, <code>sw</code> y <code>sh</code>, determinan la región a copiar desde la imagen fuente. <code>(sx, sy)</code> es la esquina superior izquierda de la región. <code>sw</code> y <code>sh</code> son el ancho y la altura de la región.</p>

  <p>Los siguientes cuatro parámetros, <code>dx</code>, <code>dy</code>, <code>dw</code> y <code>dh</code>, determinan la región del lienzo a copiar. <code>(dx, dy)</code> es la esquina superior izquierda de la región. <code>dw</code> y <code>dh</code> son el ancho y la altura de la región.</p>
line: '288'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // Load the image.
    function preload() {
      img = loadImage('assets/rockies.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // Use the mountains as the background.
      background(img);

      // Copy a region of pixels to another spot.
      copy(img, 7, 22, 10, 10, 35, 25, 50, 50);

      // Outline the copied region.
      stroke(255);
      noFill();
      square(7, 22, 10);

      describe('An image of a mountain landscape. A square region is outlined in white. A larger square contains a pixelated view of the outlined region.');
    }
    </code>
    </div>
class: p5
overloads:
  - line: '288'
    params:
      - name: srcImage
        description: |2-
    
          <p>imagen fuente.</p>
        type: p5.Image|p5.Element
      - name: sx
        description: |2-
    
          <p>coordenada x de la esquina superior izquierda de la imagen fuente.</p>
        type: Integer
      - name: sy
        description: |2-
    
          <p>coordenada y de la esquina superior izquierda de la imagen fuente.</p>
        type: Integer
      - name: sw
        description: |2-
    
          <p>ancho de la imagen fuente.</p>
        type: Integer
      - name: sh
        description: |2-
    
          <p>alto de la imagen fuente.</p>
        type: Integer
      - name: dx
        description: |2-
    
          <p>coordenada x de la esquina superior izquierda del destino.</p>
        type: Integer
      - name: dy
        description: |2-
    
          <p>coordenada y de la esquina superior izquierda del destino.</p>
        type: Integer
      - name: dw
        description: |2-
    
          <p>ancho de la imagen de destino.</p>
        type: Integer
      - name: dh
        description: |2-
    
          <p>alto de la imagen de destino.</p>
        type: Integer
  - line: '345'
    params:
      - name: sx
        description: ''
        type: Integer
      - name: sy
        description: ''
        type: Integer
      - name: sw
        description: ''
        type: Integer
      - name: sh
        description: ''
        type: Integer
      - name: dx
        description: ''
        type: Integer
      - name: dy
        description: ''
        type: Integer
      - name: dw
        description: ''
        type: Integer
      - name: dh
        description: ''
        type: Integer
chainable: false
---

# copy
