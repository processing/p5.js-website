---
title: createElement
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>Crea un nuevo objeto <a href="/reference/p5/p5.Element">p5.Element</a>.</p>

  <p>El primer parámetro, <code>tag</code>, es una cadena que representa una etiqueta HTML, como 
  <code>'h5'</code>.</p>

  <p>El segundo parámetro, <code>content</code>, es opcional. Es una cadena que 
  establece el 
  
  contenido HTML que se insertará en el nuevo elemento. Los nuevos elementos no tienen contenido 
  
  por defecto.</p>
line: 2467
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Create an h5 element with nothing in it.
      createElement('h5');

      describe('A gray square.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Create an h5 element with the content "p5*js".
      let h5 = createElement('h5', 'p5*js');

      // Set the element's style and position.
      h5.style('color', 'deeppink');
      h5.position(30, 15);

      describe('The text "p5*js" written in pink in the middle of a gray square.');
    }
    </code>
    </div>
class: p5
params:
  - name: tag
    description: |
      <p>etiqueta para el nuevo elemento.</p>
    type: String
  - name: content
    description: |
      <p>contenido HTML para insertar en el elemento.</p>
    type: String
    optional: true
return:
  description: nuevo objeto <a href="#/p5.Element">p5.Element</a>.
  type: p5.Element
chainable: false
---


# createElement
