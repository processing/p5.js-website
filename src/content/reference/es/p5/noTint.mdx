---
title: noTint
module: Imagen
submodule: Cargando y Mostrando
file: src/image/loading_displaying.js
description: |2-

  <p>Elimina el tinte actual establecido por <a href="/reference/p5/tint">tint()</a>.</p>

  <p><code>noTint()</code> restaura las imágenes a sus colores originales.</p>
line: '1345'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // Load the image.
    function preload() {
      img = loadImage('assets/laDefense.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // Left.
      // Tint with a CSS color string.
      tint('red');
      image(img, 0, 0);

      // Right.
      // Remove the tint.
      noTint();
      image(img, 50, 0);

      describe('Two images of an umbrella and a ceiling side-by-side. The image on the left has a red tint.');
    }
    </code>
    </div>
class: p5
chainable: false
---

# noTint
