---
title: getURLPath
module: Ambiente
submodule: Ambiente
file: src/core/environment.js
description: |-
  <p>Devuelve la <a href=https://developer.mozilla.org/en-US/docs/Learn/Common_questions/Web_mechanics/What_is_a_URL#path_to_resource target="_blank">ruta de URL</a>  del boceto (sketch) como un arreglo de cadenas.</p>
  <p>Por ejemplo, considera un boceto alojado en la URL <code>https:"//example.com/sketchbook</code>. Si llamamos a  <code>getURLPath()</code> obtendremos <code>['sketchbook']</code>.</p>"
  <p>En otro ejemplo, para un boceto alojado en la URL <code>https:"//example.com/sketchbook/monday</code>, al llamar a <code>getURLPath()</code>, obtendremos <code>['sketchbook', 'monday']</code>.</p>"
line: '1166'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      background(200);

      // Get the sketch's URL path
      // and display the first
      // part.
      let path = getURLPath();
      text(path[0], 25, 54);

      describe('The word "reference" written in black on a gray background.');
    }
    </code>
    </div>
class: p5
return:
  description: elementos de la ruta URL.
  type: String[]
chainable: false
---

# getURLPath
