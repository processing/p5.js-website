---
title: atan
module: Matemáticas
submodule: Trigonometría
file: src/math/trigonometry.js
description: "\n<p>Calcula el arco tangente de un número.</p>\n\n\n<p><code>atan()</code> es el inverso de <a\nhref=\"/reference/p5/tan\">tan()</a>. \n\nEspera valores de entrada en el rango de -Infinito a Infinito. Por defecto, <code>atan()</code>\ndevuelve\nvalores en el rango -π ÷ 2 (aproximadamente -1,57) a π ÷ 2 (alrededor de 1,57). \n\nSi <a href=\"/reference/p5/angleMode\">angleMode()</a> es\n<código>DEGREES</código>,\ndevuelve valores en el rango de -90 a 90.</p>"
line: '142'
isConstructor: 'false'
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate tan() and atan() values.
      let a = PI / 3;
      let t = tan(a);
      let at = atan(t);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(t, 3)}`, 35, 50);
      text(`${round(at, 3)}`, 35, 75);

      describe('The numbers 1.047, 1.732, and 1.047 written on separate rows.');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // Calculate tan() and atan() values.
      let a = PI + PI / 3;
      let t = tan(a);
      let at = atan(t);

      // Display the values.
      text(`${round(a, 3)}`, 35, 25);
      text(`${round(t, 3)}`, 35, 50);
      text(`${round(at, 3)}`, 35, 75);

      describe('The numbers 4.189, 1.732, and 1.047 written on separate rows.');
    }
    </code>
    </div>
class: p5
params:
  - name: value
  description: |2-

    <p>valor cuyo arco tangente se va a calcular.</p>
  type: Número
return:
  description: arco tangente del valor dado.
  type: Número
chainable: 'false'
---

# atan
