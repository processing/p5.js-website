---
import BaseLayout from "./BaseLayout.astro";

const { entry } = Astro.props;
---

<BaseLayout title={entry.data.title} variant="secondary">
  <div set:html={entry.data.description} class="[&_a]:underline" />
</BaseLayout>
