---
import BaseLayout from "./BaseLayout.astro";
import { removeLocaleAndExtensionFromId } from "@pages/_utils";
import type { ArchiveCollectionEntry } from "@content/types";

interface Props {
  entries: ArchiveCollectionEntry[];
  title: string;
}

const { entries, title } = Astro.props;
---

<BaseLayout title={title}>
  <ul>
    {
      entries.map((entry) => (
        <li>
          <a href={removeLocaleAndExtensionFromId(entry.id)}>
            {removeLocaleAndExtensionFromId(entry.id)}
          </a>
        </li>
      ))
    }
  </ul>
</BaseLayout>
