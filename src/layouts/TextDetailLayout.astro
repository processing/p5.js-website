---
import Head from "@components/Head/index.astro";
import BaseLayout from "./BaseLayout.astro";
import type { CollectionEntry } from "astro:content";
<<<<<<< HEAD
import { render } from "astro:content";
import { setJumpToState } from "../globals/state";
=======
>>>>>>> 64ce2c96f (Update "text detail" pages to pass "jumpToState" as a prop to <BaseLayout>, rather than use global "jumpToState" variable)
import { getCurrentLocale } from "../i18n/utils";

interface Props {
  page: CollectionEntry<"text-detail">;
}

const { page } = Astro.props;
const { Content } = await render(page);

const pageTopic = Astro.url.pathname.includes("donate")
  ? "contribute"
  : undefined;

const currentLocale = getCurrentLocale(Astro.url.pathname);

const { showBanner, englishUrl } = checkTranslationBanner(
  'text-detail',
  page.id,
  currentLocale,
  Astro.url.pathname,
  Astro.url.origin
);

<Head title={page.data.title} locale={currentLocale} />

<BaseLayout
  title={page.data.title}
  variant="item"
  topic={pageTopic}
  className={pageTopic}
  subtitle={null}
  jumpToState={null}
>
  <div class="max-w-[770px] [&>*:first-child]:mt-0 rendered-markdown pb-[80px]">
    <Content />
  </div>
</BaseLayout>
