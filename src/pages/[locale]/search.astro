---
import { nonDefaultSupportedLocales } from "@i18n/const";
import SearchLayout from "@/src/layouts/SearchLayout.astro";

export const getStaticPaths = async () => {
  return nonDefaultSupportedLocales.map((locale) => {
    return { params: { locale } };
  });
};
---

<SearchLayout title="Search Results" />
