---
const pathname = Astro.url.pathname;

const splitPathname = pathname.split("/");
const item = splitPathname[splitPathname.length - 1];

const { title } = Astro.props;
---

<div class="p-md pt-2xl">
  <h1 class="text-black">{title}</h1>
</div>
